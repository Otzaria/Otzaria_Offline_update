# תכנון מוצר ומימוש — מנהל העדכונים המקוון/לא־מקוון של אוצריא

סטטוס: טיוטת תכנון ראשונה  
עודכן: 5 באוגוסט 2026  
יעד עיקרי: Windows, עם דגש על משתמשים שמחשב אוצריא שלהם אינו מחובר לאינטרנט

## 1. מטרת המוצר

האפליקציה נועדה לאפשר ניהול ועדכון של שלושה סוגי תוכן הקשורים לאוצריא:

1. תוכנת אוצריא עצמה.
2. ספריית הספרים (`seforim.db`).
3. תוספי אוצריא.

על האפליקציה לעבוד בשני תרחישים משלימים:

- **מחשב מקוון:** זיהוי המצב המקומי, בדיקת עדכונים ברשת, הורדה למחשב עצמו או הכנת כונן USB להעברה.
- **מחשב לא־מקוון:** זיהוי אוצריא, הספרייה והתוספים המותקנים, קריאת חבילת עדכון מ־USB והחלת העדכונים ללא פנייה לרשת.

האפליקציה לא תתחיל כברירת מחדל הורדות או התקנות ללא פעולה מפורשת של המשתמש. ניתן יהיה להפעיל בהגדרות אוטומציה מדורגת וברורה.

## 2. החלטות מוצר בסיסיות

### 2.1 מספר המסכים

הדרישה מפרטת בפועל ארבעה אזורי תוכן. לכן התכנון מגדיר **ארבעה מסכים עליונים**:

1. דף הבית — מצב כללי ועדכון תוכנת אוצריא.
2. עדכון ספרייה.
3. עדכון תוספים.
4. הגדרות.

אם רוצים להישאר עם שלושה מסכים בלבד, ניתן להציג את ההגדרות כחלונית צד או דיאלוג. מבחינת תוכן ופיתוח ההגדרות עדיין מהוות יחידה עצמאית, ולכן עדיף להשאיר אותן כמסך רביעי.

### 2.2 ברירות מחדל

- אין הורדה אוטומטית, אלא אם המשתמש סימן זאת בהגדרות.
- אין התקנה אוטומטית - גם זאת יוכל המשתמש להגדיר.
- מוצגת בדיקה מקומית תמיד.
- אם יש רשת, מתבצעת בדיקת מטא־דאטה קלה בלבד: גרסאות, גדלים ותאריכי פרסום; לא מורידים נכסים כבדים.
- כל התקנה או החלפת DB דורשת אישור, אלא אם המשתמש הפעיל במפורש אוטומציה מתאימה.
- גרסאות prerelease/preview אינן נבחרות כברירת מחדל. הפעלתן דורשת בחירה מפורשת והצגת אזהרה.

### 2.3 הפרדה בין הורדה להתקנה

יש להפריד במודל ובממשק בין:

- בדיקת עדכון.
- הורדת העדכון למטמון.
- הכנת העדכון להעברה.
- התקנת העדכון במחשב הנוכחי.

כך ניתן להוריד במחשב מקוון, להעביר ב־USB, ורק לאחר מכן להתקין במחשב אחר.

## 3. משתמשים ותרחישים עיקריים

### 3.1 משתמש במחשב מקוון

רוצה לדעת מה מותקן, לראות מה חדש, לעדכן את המחשב או להכין כונן למחשב לא־מקוון.

### 3.2 משתמש במחשב לא־מקוון

רוצה לחבר USB, לראות אילו עדכונים נמצאו עליו, לבחור מה לעדכן ולבצע עדכון בטוח עם אפשרות התאוששות.

### 3.3 מכין עדכונים עבור כמה מחשבים

רוצה להוריד פעם אחת תוכנה, ספרייה ותוספים, לשמור חבילה ניידת מלאה או חלקית, ולעדכן ממנה כמה מחשבים ללא הורדות חוזרות.

## 4. מבנה הניווט

ב־Windows מומלץ להשתמש ב־`NavigationRail` או סרגל צד קבוע, ולא ב־pull-to-refresh נסתר:

```text
┌──────────────────────────────────────────────────────────┐
│ אוצריא — מנהל עדכונים                   רשת / USB / עזרה │
├──────────────┬───────────────────────────────────────────┤
│ דף הבית      │                                           │
│ ספרייה       │              תוכן המסך                   │
│ תוספים       │                                           │
│ הגדרות       │                                           │
└──────────────┴───────────────────────────────────────────┘
```

בחלק העליון יוצגו תמיד:

- מצב רשת: מחובר, לא מחובר, בודק או שגיאה.
- מקור העדכונים הפעיל: אינטרנט, מטמון מקומי או כונן USB.
- התראה אם אוצריא פתוחה וחוסמת עדכון.
- קיצור לפתיחת יומן הפעילות.

## 5. דף הבית

### 5.1 מטרות

- לתת תמונת מצב אחת ברורה.
- לזהות התקנת אוצריא קיימת ולא ליצור עותק נוסף ללא צורך.
- להציג את מצב התוכנה, הספרייה והתוספים.
- לאפשר עדכון תוכנת אוצריא.
- להציע הכנת USB או קריאת USB, בהתאם למקור הזמין.

### 5.2 מצב מקומי

כאשר אוצריא מותקנת יוצגו:

- נתיב ההתקנה שזוהה.
- גרסה מותקנת מלאה.
- ערוץ הגרסה: יציב או preview.
- מצב התהליך: פתוחה או סגורה.
- נתיב הספרייה הפעילה וגרסת ה־DB.
- מספר התוספים המותקנים.
- רשימת תקציר של תוספים: שם, גרסה, מצב תקינות ועדכון זמין.
- תאריך העדכון המקומי האחרון, אם ידוע.

אם אוצריא אינה מזוהה:

- כפתור “אתר התקנה קיימת”.
- כפתור “התקן אוצריא”.
- הסבר קצר שלא תתבצע התקנה לפני אישור.

הזיהוי צריך לבדוק, לפי סדר:

1. נתיב שנשמר בעבר באפליקציה.
2. Windows Registry, אם המתקין רושם שם התקנה.
3. מיקומים ידועים של אוצריא.
4. בחירה ידנית של תיקייה או קובץ `otzaria.exe`.

כל נתיב שמור חייב להיבדק מחדש בכל הפעלה; אין להציג “מותקן” אם קובץ ה־exe נעלם.

### 5.3 מידע מהרשת

כאשר קיימת רשת יוצגו, ללא הורדה אוטומטית:

- גרסת אוצריא האחרונה והערוץ שלה.
- גרסת הספרייה האחרונה.
- מספר עדכוני התוספים הזמינים.
- גודל משוער של כל עדכון ושל הבחירה כולה.
- תאריך פרסום וקישור לרשימת שינויים, אם קיים.
- חיווי אם המטמון המקומי או ה־USB כבר מכילים את אותה גרסה.

כאשר אין רשת:

- המידע המקומי נשאר זמין במלואו.
- אם יש מטא־דאטה שמור מבדיקה קודמת, הוא יוצג כ“מידע שנבדק לאחרונה בתאריך…”.
- כפתורי הפעלה, איתור התקנה ועדכון מ־USB יישארו פעילים.
- שגיאת רשת לא תהפוך את כל כרטיס אוצריא למצב שגיאה.

### 5.4 פעולות עיקריות

- הפעלת אוצריא.
- בדיקה מחדש.
- הורדת עדכון תוכנה.
- התקנת עדכון תוכנה שכבר הורד.
- פתיחת פירוט השינויים.
- הכנת כונן עדכון.
- טעינת חבילת עדכון מכונן.

## 6. מסך עדכון ספרייה

### 6.1 מידע שיוצג

- נתיב `seforim.db` הפעיל בפועל.
- גרסה מקומית, schema version וגודל הקובץ.
- גרסה זמינה במקור הפעיל.
- סוג העדכון המתוכנן: דלתא, שרשרת דלתאות או DB מלא.
- גודל הורדה וגודל דיסק נדרש.
- מקור: רשת, מטמון או USB.
- מועד גיבוי/עדכון אחרון.
- מצב תקינות: תקין, דורש שחזור או גרסה לא מזוהה.

### 6.2 פעולות

- בדיקת עדכונים.
- הורדה בלבד.
- הורדה והתקנה.
- התקנה מחבילה שכבר קיימת.
- בחירת DB אחר.
- פתיחת תיקיית הספרייה.
- בדיקת תקינות.
- שחזור מגיבוי אחרון, אם קיים.

### 6.3 כללי בטיחות

- אין לעדכן בזמן שאוצריא פתוחה.
- לפני החלפת DB מלא יש לבדוק מקום פנוי עבור קובץ חדש, גיבוי וקבצים זמניים.
- דלתא תוחל בטרנזקציה ותאומת מול hash יעד.
- החלפה מלאה תתבצע דרך קובץ זמני ו־rename אטומי ככל האפשר.
- יש לשמור גיבוי עד שהגרסה החדשה עברה פתיחה, בדיקת גרסה ו־`quick_check`.
- חילוץ DB מלא חייב להיות streaming ולא טעינת כל הקובץ לזיכרון.
- המשתמש יקבל סיכום ברור לפני התחלה ולא הודעת חריג גולמית.

### 6.4 התקנה טרייה

אם אין DB קיים, אין לכתוב אוטומטית לנתיב פנימי של הלאנצ׳ר בלי לחבר אותו לאוצריא. יש לבחור אחת משתי מדיניות:

1. כתיבה לנתיב ברירת המחדל שאוצריא עצמה קוראת ממנו.
2. כתיבה לנתיב שנבחר, ועדכון הגדרות אוצריא כך שתשתמש בו.

ההתקנה תיחשב מוצלחת רק לאחר שאומת שאוצריא תראה בפועל את ה־DB החדש.

## 7. מסך עדכון תוספים

זהו מודול חדש שעדיין אינו קיים במאגר.

### 7.1 דרישות מקדימות

לפני המימוש יש להגדיר חוזה רשמי לתוסף:

- מזהה קבוע וייחודי.
- שם ותיאור.
- גרסה בפורמט מוגדר.
- גרסת אוצריא מינימלית ומרבית.
- מערכת הפעלה/ארכיטקטורה נתמכות.
- רשימת קבצים ונתיב התקנה.
- תלויות בתוספים אחרים.
- hash או חתימה לכל חבילה.
- מקור רשמי ומדיניות אמון.
- הוראות התקנה, שדרוג והסרה.
- האם נשמר מידע משתמש שאסור למחוק בעדכון.

### 7.2 זיהוי תוספים מותקנים

יש לממש `InstalledPluginsScanner` שקורא את מנגנון התוספים האמיתי של אוצריא ולא מנחש לפי שמות תיקיות בלבד. הסורק יחזיר:

- מזהה תוסף.
- שם.
- גרסה מותקנת.
- נתיב.
- מצב: תקין, חסרים קבצים, לא מזוהה או לא תואם לגרסת אוצריא.
- מקור ההתקנה, אם ידוע.

### 7.3 תצוגה

המסך יכלול:

- חיפוש וסינון: מותקן, עדכון זמין, לא מותקן, לא תואם או שגיאה.
- שורה לכל תוסף עם גרסה מקומית וגרסה זמינה.
- בחירה מרובה.
- פירוט גודל, הרשאות, תלות ושינויים.
- סימון ברור לתוסף ניסיוני או לא חתום.

### 7.4 פעולות

- עדכון כל התוספים המותקנים.
- עדכון תוספים מסומנים.
- הורדת כל התוספים לחבילת offline, גם אם אינם מותקנים.
- התקנת תוסף חדש.
- הסרה או השבתה, אם אוצריא תומכת בכך.
- שחזור גרסה קודמת במקרה של כשל.

### 7.5 התקנה בטוחה

- יש לבנות תוכנית תלויות לפני שינוי קבצים.
- יש להוריד ולאמת את כל החבילות לפני תחילת ההתקנה.
- אין להשאיר חצי התקנה: staging, גיבוי והחלפה אטומית.
- יש לשמור קבצי משתמש והגדרות לפי חוזה התוסף.
- כשל בתוסף אחד לא יפגע בתוספים שכבר היו תקינים.
- לאחר ההתקנה תתבצע סריקה מחדש והשוואת גרסאות.

## 8. מסך הגדרות

### 8.1 אוטומציה

האוטומציה תוגדר באמצעות אפשרויות בלתי תלויות שניתן לשלב:

| אפשרות | ברירת מחדל | משמעות |
|---|---:|---|
| בדיקת מטא־דאטה אוטומטית | פעיל | בדיקה קלה בלבד, ללא הורדה |
| הורדת עדכון תוכנת אוצריא | כבוי | מוריד גרסה חדשה למטמון |
| התקנת עדכון תוכנת אוצריא | כבוי | מתקין לאחר הורדה; דורש אוצריא סגורה |
| הורדת עדכון ספרייה | כבוי | מוריד דלתא/DB למטמון |
| התקנת עדכון ספרייה | כבוי | מחיל עדכון בטוח על ה־DB |
| הורדת עדכוני תוספים מותקנים | כבוי | רק תוספים שזוהו במחשב |
| התקנת עדכוני תוספים מותקנים | כבוי | מתקין לאחר אימות |
| הורדת כלל התוספים | כבוי | מיועד בעיקר להכנת חבילת USB מלאה |
| הכנת חבילת USB אוטומטית | כבוי | מסנכרן ליעד נבחר כשהוא מחובר |
| שימוש בגרסאות preview | כבוי | לכל רכיב בנפרד או באופן כללי |

יש להבחין בממשק בין **הורדה אוטומטית** לבין **התקנה אוטומטית**. הפעלת התקנה אוטומטית תדרוש אישור חד־פעמי והסבר על הסיכון.

### 8.2 בחירות ערוץ

לכל תחום ניתן לבחור:

- יציב בלבד.
- יציב או preview.
- גרסה נעוצה ידנית, אם נדרש בארגון או בבית מדרש.

בחירה ב־preview של תוכנת אוצריא לא תחייב אוטומטית preview של הספרייה או התוספים.

### 8.3 נתיבים ואחסון

- נתיב התקנת אוצריא.
- נתיב הספרייה.
- תיקיית התוספים.
- תיקיית מטמון.
- יעד USB מועדף.
- גודל מטמון מרבי.
- מספר גיבויים לשמירה.
- ניקוי קבצים ישנים.

### 8.4 רשת

- עבודה עם proxy, אם נדרש.
- timeout ומספר ניסיונות חוזרים.
- הגבלת הורדה בחיבור מדוד.
- אפשרות “עבודה offline בלבד”.
- כפתור בדיקת חיבור ומקורות.

### 8.5 ממשק ותמיכה

- מצב בהיר/כהה או התאמה למערכת.
- גודל טקסט.
- פתיחת תיקיית לוגים.
- יצירת חבילת אבחון ללא מידע רגיש.
- איפוס הגדרות מקור/נתיבים בלי למחוק התקנות.

## 9. חבילת העדכון הניידת

יש להגדיר פורמט אחיד לכל תוכן ה־USB, במקום mirror נפרד לספרייה בלבד.

מבנה מוצע:

```text
otzaria-update/
├── update-manifest.json
├── launcher/
│   └── launcher_app.exe + קובצי runtime
├── application/
│   ├── releases.json
│   └── <version>/installer.exe
├── library/
│   ├── releases.json
│   └── assets/...
├── plugins/
│   ├── catalog.json
│   └── <plugin-id>/<version>/package...
└── integrity/
    ├── checksums.json
    └── signature
```

`update-manifest.json` יכלול:

- גרסת פורמט החבילה.
- מועד יצירה.
- גרסת Launcher נדרשת.
- רשימת רכיבים וגרסאות.
- גדלים ו־hashes.
- ערוצים שנכללו.
- האם החבילה מלאה או חלקית.
- מזהה ייחודי לצורך resume וסנכרון.
- חתימה דיגיטלית או הפניה למנגנון האמון.

### 9.1 הכנת USB

זרימה מומלצת:

1. בחירת כונן יעד.
2. סריקת תוכן קיים בכונן.
3. בחירת רכיבים: תוכנה, ספרייה, תוספים מותקנים או כל התוספים.
4. הצגת גודל הורדה ומקום פנוי.
5. הורדת קבצים חסרים בלבד.
6. אימות כל הקבצים.
7. כתיבת manifest חדש דרך קובץ זמני ו־rename.
8. הצגת “הכונן מוכן” עם אפשרות הוצאה בטוחה.

אין להוריד מחדש קובץ שכבר קיים ועבר אימות. אין לכתוב ישירות על manifest תקין לפני שכל הנכסים החדשים הושלמו.

### 9.2 עדכון מ־USB

1. זיהוי אוטומטי של כוננים מחוברים המכילים `update-manifest.json`.
2. אימות פורמט, חתימה ו־hashes.
3. השוואה למצב המקומי.
4. הצגת תוכנית עדכון ובחירה מפורשת.
5. בדיקת מקום פנוי ואוצריא סגורה.
6. התקנה לפי סדר: תוכנת אוצריא, ספרייה, תוספים — או סדר אחר שייקבע לפי חוזי תאימות.
7. אימות לאחר כל רכיב.
8. דו״ח סיום מפורט ואפשרות שמירת לוג על ה־USB.

יש לתכנן מצב שבו עדכון תוכנת אוצריא דורש הפעלה מחדש של מנהל העדכונים לפני המשך הספרייה או התוספים.

## 10. מודל מצב ומקורות מידע

### 10.1 מצב מקומי

מומלץ לשמור state אחד בעל schema version, למשל:

```json
{
  "schemaVersion": 1,
  "otzariaInstallPath": "...",
  "libraryPath": "...",
  "pluginsPath": "...",
  "preferredUsbPath": "...",
  "lastOnlineCheck": "...",
  "automation": {},
  "channels": {},
  "cachePolicy": {}
}
```

הכתיבה תהיה אטומית, עם migration בין גרסאות schema ושחזור מקובץ גיבוי במקרה של JSON פגום.

### 10.2 סדר עדיפות למקור עדכון

אין לבחור מקור באופן סמוי. הממשק יציג את המקור, והלוגיקה תפעל לפי:

1. חבילת USB שהמשתמש בחר או אישר.
2. מטמון מקומי תקין.
3. רשת, אם זמינה ומותרת.

אפשר להשוות בין המקורות, אך אין להחליף אוטומטית USB בגרסה אחרת מהרשת באמצע תוכנית עדכון.

### 10.3 זיהוי רשת

“יש רשת” אינו רק קיום כרטיס רשת. יש לבצע בקשת metadata קצרה למקור הנבחר עם timeout. מצב הרשת יהיה אחד מאלה:

- online.
- offline.
- captive portal / proxy error.
- GitHub/API unavailable.
- rate limited.

המערכת המקומית תמשיך לעבוד בכל המצבים.

## 11. ארכיטקטורה מוצעת

### 11.1 חבילות

```text
launcher_app/                 UI, ניווט ותזמור
otzaria_manager/              תוכנת אוצריא: זיהוי, הורדה, cache והתקנה
library_manager/              איתור DB והחלת עדכון
seforim_library_updater/       חוזה הדלתאות וה־hash
plugins_manager/              קטלוג, זיהוי, תכנון והתקנת תוספים — חדש
offline_bundle/               manifest אחיד, סנכרון USB ואימות — חדש
update_core/                  מדיניות cache, מקורות, jobs ואוטומציה — אופציונלי
```

### 11.2 שירותים חדשים מרכזיים

- `OtzariaInstallationLocator` — זיהוי אמין של התקנה קיימת.
- `NetworkStatusService` — בדיקת זמינות מקורות ללא חסימת מצב מקומי.
- `UpdateMetadataCache` — מידע אחרון מהשרת, כולל timestamp.
- `DownloadCacheManager` — הורדה, resume, אימות, מגבלת גודל וניקוי.
- `OfflineBundleReader` / `OfflineBundleWriter` — פורמט USB אחיד.
- `OfflineBundleSynchronizer` — הורדת קבצים חסרים בלבד וכתיבה אטומית.
- `InstalledPluginsScanner` — זיהוי תוספים.
- `PluginCatalogClient` — קטלוג מקוון/מקומי.
- `PluginUpdatePlanner` — תאימות ותלויות.
- `PluginInstaller` — staging, rollback ואימות.
- `DiskSpaceService` — חישוב מקום נדרש מול מקום פנוי.
- `UpdateJobCoordinator` — מניעת פעולות מתנגשות, ביטול והמשך.
- `SettingsStore` — הגדרות versioned וכתיבה אטומית.

### 11.3 משימות עדכון

כל פעולה ארוכה תיוצג כ־job בעל:

- מזהה.
- סוג רכיב.
- מקור ויעד.
- שלב נוכחי.
- התקדמות בבייטים ובפריטים.
- יכולת ביטול.
- יכולת resume, אם אפשר.
- שגיאה ידידותית וקוד טכני ללוג.
- תוצאת אימות.

לא יופעלו במקביל שתי פעולות שכותבות לאותו DB, לאותה תיקיית תוספים או לאותו USB.

## 12. אבטחה ואמון

- כל קובץ שהורד חייב לעבור אימות cryptographic, לא רק בדיקת גודל.
- עדיף manifest חתום במפתח ציבורי שמוטמע באפליקציה.
- installer של אוצריא ייבדק גם מול חתימת Authenticode, אם קיימת.
- חבילת USB תיחשב לא מהימנה עד לאחר אימות manifest וכל הנכסים שנבחרו.
- אין לבצע התקנה אוטומטית של preview ללא opt-in מפורש.
- אין להריץ installer ממקור לא מוכר או מנתיב שניתן לשנות בזמן האימות.
- יש להגן מפני path traversal בשמות נכסים ותוספים.
- אין לכתוב סודות, query parameters או מידע אישי ללוג.
- תוצג למשתמש זהות המקור: רשמי, מקומי חתום או מקומי לא מאומת.

## 13. אמינות והתאוששות

- הורדות ייכתבו לקובצי `.partial` ורק לאחר אימות יועברו לשם הסופי.
- manifests ייכתבו אחרונים ובאופן אטומי.
- יהיה journal של תוכנית העדכון לצורך התאוששות לאחר הפסקת חשמל.
- כל רכיב יגדיר rollback משלו.
- אוצריא תיבדק שוב לפני שלב הכתיבה, לא רק בתחילת ההורדה.
- יש לטפל בניתוק USB בזמן הורדה או התקנה.
- יש להציג מראש מקום נדרש, כולל גיבוי וקבצים זמניים.
- יש להגביל retry עם backoff ולא ליצור לולאת הורדה אינסופית.
- סגירת האפליקציה בזמן job תציג אזהרה; הורדה ניתנת לעצירה, כתיבה קריטית לא תופסק בצורה מסוכנת.

## 14. עיצוב, נגישות ושפה

- RTL מלא בכל widgets, dialogs ו־file pickers שבשליטת האפליקציה.
- רוחב תוכן מרבי במסכי desktop כדי למנוע כרטיסים רחבים מדי.
- אין להסתמך על צבע בלבד; לכל מצב יהיה סמל וטקסט.
- גופנים ייארזו בתוך האפליקציה ולא ייטענו מהאינטרנט בזמן ריצה.
- תמיכה ב־keyboard navigation, focus ברור ו־screen readers.
- טקסטי שגיאה יהיו בעברית פשוטה עם “מה ניתן לעשות עכשיו”.
- פרטים טכניים ו־stack trace יוצגו רק באזור “פרטים נוספים” או בלוג.
- כל פעולה אוטומטית תוצג בפעילות האחרונה ולא תתרחש בשקט מוחלט.

## 15. לוגים, פרטיות ואבחון

יש לשמור:

- הפעלה וסגירה.
- מקורות שנבדקו.
- גרסאות לפני ואחרי.
- תוכנית עדכון שנבחרה.
- הורדות, אימותים ותוצאות התקנה.
- פעולות rollback.
- שגיאות עם stack trace.

אין לשלוח telemetry כברירת מחדל. אם יתווסף בעתיד דיווח תקלות, הוא יהיה opt-in ויציג למשתמש בדיוק מה נשלח.

הלוג יעבור rotation לפי גודל/מספר ימים, כדי שלא יצטבר ללא גבול.

## 16. בדיקות נדרשות

### 16.1 Unit

- parsing ו־migration של state/settings/manifests.
- בחירת מקורות וערוצים.
- חישוב אוטומציה.
- תכנון עדכון תוספים ותלויות.
- חישוב מקום פנוי.
- סינון גרסאות יציבות/preview.

### 16.2 Integration

- יצירת bundle, קריאתו והשוואת hashes.
- סנכרון חוזר שאינו מוריד קבצים קיימים.
- ניתוק רשת וחידוש הורדה.
- ניתוק USB באמצע סנכרון.
- עדכון DB דלתאי ומלא מול fixtures אמיתיים.
- rollback של תוכנה, ספרייה ותוספים.
- זיהוי התקנת אוצריא אמיתית ותוספים אמיתיים.

### 16.3 Widget ו־UX

- כל מצב מסך: אין התקנה, offline, online, update available, downloading, installing, blocked ו־error.
- ניווט מלא במקלדת.
- טקסטים ארוכים ונתיבים ארוכים.
- DPI גבוה ושינוי גודל חלון.
- RTL ו־Semantics.
- אין הורדה אוטומטית כאשר כל אפשרויות האוטומציה כבויות.

### 16.4 Windows end-to-end

- Windows 10 ו־11.
- התקנה רגילה ו־portable.
- משתמש ללא הרשאות Administrator.
- נתיבים בעברית וברווחים.
- FAT32, exFAT ו־NTFS; יש לחסום מראש קובץ גדול מ־4GB ב־FAT32.
- אוצריא פתוחה בזמן ניסיון עדכון.
- עדכון ממחשב מקוון ל־USB ומשם למחשב מנותק אמיתי.

## 17. אריזה, הפצה ו־CI

- לשמור `windows/` במאגר ולא ליצור scaffolding חדש בכל build.
- לבחור פורמט הפצה רשמי אחד לפחות: portable ZIP מתאים במיוחד ל־USB.
- אם נשמר גם installer, לתקן ולהגדיר בפועל את `inno_bundle` או כלי חלופי.
- לשמור `pubspec.lock` עבור האפליקציות.
- להוסיף `.gitignore` תקין.
- לבנות ולבדוק portable ו־installer ב־CI.
- לפרסם artifacts כ־GitHub Release עם גרסה, checksum וחתימה; לא להסתמך רק על artifacts זמניים של Actions.
- להוסיף build number וגרסה גלויים במסך “אודות”.

## 18. שלבי מימוש מוצעים

### שלב 0 — החלטות וחוזים

- לאמת את מיקום ההתקנה, הספרייה והתוספים מול אוצריא בפועל.
- להגדיר פורמט תוסף וקטלוג רשמי.
- להחליט על ערוצי stable/preview.
- להחליט על מפתח חתימה ומקור אמון.
- לאשר שהמוצר כולל ארבעה מסכים או שלושה והגדרות כדיאלוג.

### שלב 1 — בסיס UI ומצב מקומי

- ניווט חדש וארבעת המסכים.
- זיהוי אמין של אוצריא, DB ותוספים.
- הפרדת מצב מקומי ממצב רשת.
- מסך הגדרות ללא אוטומציה פעילה כברירת מחדל.
- תיקון האפשרות להפעיל אוצריא גם ללא רשת.

### שלב 2 — הורדה ומטמון

- cache manager משותף.
- metadata cache.
- הפרדת download/install.
- גודל, מקום פנוי, cancel ו־resume.
- הורדה דיפרנציאלית בלבד.

### שלב 3 — חבילת USB אחידה

- `offline_bundle` ו־manifest חתום.
- הכנת USB מתוך הממשק.
- זיהוי וקריאת USB במחשב היעד.
- תוכנה וספרייה באותה חבילה.
- כתיבה אטומית וסנכרון חוזר.

### שלב 4 — תוספים

- סורק תוספים מותקנים.
- קטלוג מקוון/מקומי.
- תכנון תאימות ותלויות.
- התקנה, עדכון, rollback וחבילת “כל התוספים”.

### שלב 5 — אוטומציה

- מנוע מדיניות לפי אפשרויות ההגדרות.
- הפרדה בין הורדה להתקנה אוטומטית.
- תזמון, מניעת jobs מתנגשים וסיכום פעילות.
- בדיקות מפורשות שכל ברירות המחדל פסיביות.

### שלב 6 — הקשחה והפצה

- streaming ל־DB מלא.
- חתימות ואימות installer.
- בדיקות Windows end-to-end.
- נגישות, לוקליזציה וליטוש שגיאות.
- CI ירוק ו־GitHub Release חתום.

## 19. קריטריוני קבלה לגרסה ראשונה שימושית

הגרסה הראשונה תיחשב מוכנה רק אם:

- פתיחת האפליקציה אינה מורידה או מתקינה דבר בברירת המחדל.
- מצב מקומי מוצג גם ללא רשת.
- ניתן להפעיל אוצריא ללא בדיקת GitHub מוצלחת.
- התקנת אוצריא קיימת מזוהה או ניתנת לבחירה ידנית.
- גרסאות אוצריא, הספרייה והתוספים המותקנים מוצגות נכון.
- ניתן להכין USB הכולל תוכנת אוצריא, ספרייה ותוספים נבחרים.
- הכנה חוזרת מורידה רק קבצים חסרים או חדשים.
- ניתן לעדכן מחשב מנותק לחלוטין מאותו USB.
- כל נכס מאומת לפני התקנה.
- כשל או הפסקת חשמל אינם משאירים DB או תוסף במצב חלקי.
- אוצריא משתמשת בפועל ב־DB שהתעדכן.
- אין צורך להעתיק ידנית תיקיות נסתרות מתוך `%APPDATA%`.
- ה־portable build מופץ כ־Release מתועד ועובר בדיקת Windows אמיתית.

## 20. שאלות פתוחות שיש להכריע לפני מימוש התוספים

1. היכן נשמרים תוספי אוצריא כיום ומהו קובץ המטא־דאטה שלהם?
2. האם לכל תוסף כבר יש מזהה וגרסה אמינים?
3. מהו המקור הרשמי לקטלוג התוספים?
4. האם תוסף הוא archive, קובץ יחיד או installer עצמאי?
5. האם תוספים יכולים להריץ קוד, ומהו מודל האמון הרצוי?
6. כיצד נקבעת תאימות בין תוסף, גרסת אוצריא וגרסת הספרייה?
7. האם “כל התוספים” כולל גרסאות ישנות או רק את הגרסה האחרונה מכל תוסף?
8. האם נדרש לתמוך בכמה התקנות אוצריא באותו מחשב?
9. האם USB צריך לכלול תמיד גם את ה־Launcher עצמו ולעדכן אותו עצמאית?
10. האם עדכונים אוטומטיים פירושם הורדה בלבד, או שמותרת גם התקנה ללא אישור בכל רכיב?

## 21. סיכום הפערים לעומת המצב הנוכחי

כדי להגיע למוצר המתואר יש לממש או לשנות בעיקר:

- ניווט ומבנה מסכים חדש.
- ביטול ההורדה וההתקנה האוטומטיות כברירת מחדל.
- מנוע הגדרות ואוטומציה מפורט.
- הפרדה בין מצב מקומי, מידע רשת, הורדה והתקנה.
- זיהוי אמין של התקנת אוצריא קיימת והפעלה גם offline.
- חיבור נכון בין DB חדש לבין אוצריא.
- רענון mirror דיפרנציאלי ואטומי במקום הורדה מלאה בכל פתיחה.
- פורמט USB אחיד לתוכנה, ספרייה ותוספים.
- מודול תוספים מלא.
- אימות חתימות/hashes ומדיניות stable/preview.
- streaming, בדיקת מקום פנוי, ביטול, resume ו־rollback.
- הרחבה משמעותית של הבדיקות והשלמת CI/Release תקינים.

