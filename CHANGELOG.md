# Changelog

## Unreleased — החילוץ אינו נשען עוד על `tar.exe`, והלאנצ'ר מזהה ערמת קבצים שאינה שלו

בפורום דווח שהעדכון מ-0.1.8 ל-0.1.9 "תוקע את התוכנה והיא מפסיקה לעבוד"
([post/34063](https://otzaria.org/forum/post/34063)), לצד תלונה שאי אפשר להריץ
את התוכנה ישירות מהכונן הנייד. שתי התלונות הן אותה תקלה: **העדכון העצמי מחייב
חילוץ מחדש**, ובמכונות מסוימות החילוץ נכשל — או משאיר `app-files` מעורבת,
שבה `launcher_app.exe` מגרסה אחת מול `flutter_windows.dll` מאחרת. צירוף כזה
קורס מיד בעלייה, ואין ממנו דרך חזרה.

- **החילוץ עבר לתוך התהליך.** קודם ה-stub כתב zip ל-`%TEMP%` והריץ עליו את
  `tar.exe` של Windows. שתי התלויות האלה הן ששברו אותו בשטח: `tar.exe` קיים
  רק מ-Windows 10 1803, והנתיבים הועברו אליו דרך שורת פקודה שעוברת המרה
  ל-ANSI — נתיב עברי על כונן שאין בו שמות 8.3 (exFAT, וכל כונן שאינו כונן
  המערכת) הגיע אליו משובש, ולכן החילוץ נכשל שם תמיד. עכשיו ה-payload נפרש
  ישירות מהזיכרון של ה-exe אל הדיסק דרך ה-Compression API של Windows
  (`cabinet.dll`, קיים מ-Windows 8): **בלי תהליך חיצוני, בלי קובץ זמני,
  והנתיבים נשארים UTF-16 מקצה לקצה.**
- **`payload.otz` במקום zip** — מכל פשוט משלנו (טבלת קבצים + גוש דחוס
  ב-LZMS), ש-`pack_payload.ps1` כותב ו-`stub.c` קורא. LZMS גם דוחס טוב יותר
  מ-zip, כך שה-exe קטן יותר. `stub_contract_test.dart` מצמיד את החתימה ואת
  האלגוריתם לשני הצדדים, ומוודא שלא חזר תהליך חיצוני.
- **חלון "מכין את התוכנה להרצה…"** מחליף את חלון הקונסולה של tar, שהיה עד
  היום סימן החיים היחיד בהרצה הראשונה. החילוץ רץ בחוט נפרד והחלון מזרים
  הודעות — אחרת הוא היה נצבע לבן ומסומן כ"אינו מגיב" תוך שניות.
- **המרקר נכתב רק אם הלאנצ'ר באמת שם.** חילוץ שכתב 1999 קבצים מתוך 2000 אינו
  "מוכן", וזה בדיוק המצב שאסור למרקר להכריז עליו כתקין.
- **`OTZARIA_PAYLOAD_VERSION`: הלאנצ'ר בודק שהוא מה שה-exe מצפה לו.** המרקר
  מחזיק את גרסת ה-payload, אבל הוא קובץ **בתוך** התיקייה שהוא מעיד עליה:
  העתקה ידנית חלקית בין מחשבים — מה שהמשתמשים עושים כשהכונן נוסע — מעתיקה
  אותו יחד עם חלק מהקבצים, ומשאירה מרקר שמכריז "מעודכן" על ערמה מעורבת.
  ה-stub מוסר עכשיו גם את הגרסה שהוא נושא, ו-`PayloadCheck` משווה אותה לקבוע
  שנקמפל לתוך הלאנצ'ר שרץ — שני מקורות בלתי תלויים. באי-התאמה מוצג
  `PayloadMismatchScreen` (בלי "המשך בכל זאת" — זה בדיוק מה שקורס), המרקר
  נמחק, וההרצה הבאה מחלצת מחדש.
- **ל-stub יש לוג.** `app-files\OtzariaData\logs\stub.log`, לצד
  `launcher.log`: מה חולץ ולאן, מה המרקר החזיק, איזה קובץ לא נכתב ובאיזו
  שגיאה. עד עכשיו כל מה שקרה שם קרה בשקט מוחלט, וזו הסיבה שדיווח כזה לא היה
  ניתן לאבחון בכלל.
- **שורת הפתיחה של הלוג נושאת את מספר הגרסה** (`--- launcher started
  (0.1.9) ---`), וגם את גרסת ה-stub כשהיא שונה. בלעדיה אי אפשר לדעת מהלוג אם
  עדכון שהותקן באמת עלה — השאלה הראשונה בכל דיווח על עדכון שנתקע.

נבדק בפועל מול stub מקומפל שהורץ מתיקייה בשם עברי: 304 קבצים חולצו זהים
בית-בית (כולל שם קובץ בעברית ונתיב עמוק), הרצה שנייה מדלגת על החילוץ, ומרקר
ישן מפעיל חילוץ מחדש מעל התקנה חיה בלי לגעת ב-`OtzariaData` ובלי למחוק דבר.

בדיקות חדשות: `payload_check_test.dart` (התאמה, אי-התאמה בשני הכיוונים, היעדר
משתנה סביבה, נתיב המרקר, מחיקתו, ושורת הפתיחה של הלוג), חמש
ב-`stub_contract_test.dart` (משתנה הסביבה החדש, שם המרקר, תיקיית הנתונים,
מבנה המכל, ושהחילוץ אינו מריץ תהליך חיצוני), ואחת ב-`screens_test.dart`.

## Unreleased — תוכנה שנוספה ניתנת גם לעריכה

עד עכשיו הטופס "הוספת תוכנה" היה חד-כיווני: אחרי השמירה אפשר היה רק להסיר.
טעות בשם קובץ ההרצה, מיקום התקנה שלא נבחר, או שם שרוצים לתקן — כולם חייבו
מחיקה והתחלה מחדש, ואיתם ירד גם קובץ ההתקנה שכבר נשא הכונן.

- **כפתור עריכה בכל כרטיס במסך "תוכנות נוספות"**, שפותח את אותו טופס עצמו
  מלא במה שכבר נשמר. אותו טופס בכוונה: מה שאפשר למלא בהוספה חייב להיות גם
  מה שאפשר לתקן אחריה.
- **המזהה אינו ניתן לעריכה, וזה מכוון.** הוא שם התיקייה תחת `mirror/apps/`
  שבה יושבים קובץ ההתקנה השמור והמיקומים שנלמדו בכל מחשב; שינויו היה מנתק
  את התוכנה משניהם. `CustomAppsManager.update` שומר על רשומה קיימת בלבד
  ודוחה מזהה שאינו רשום — כדי ש"עריכה" לא תיצור בשקט רשומה חדשה.
- **המקור שכבר נבחר נשמר כשלא נוגעים בו.** בעריכת תוכנה מגיטהאב, כל עוד
  הריפו שבשדה הוא זה שנשמר, תבנית הקובץ נשארת כשהייתה והטופס אומר זאת
  במפורש — אין צורך להביא שוב את רשימת ה-release ולבחור מחדש. ברגע שהריפו
  שבשדה שונה, נדרשת בחירת קובץ חדשה: תבנית שנבחרה בריפו אחד אינה אומרת דבר
  על השני. בעריכת תוכנה מקובץ, קובץ ההתקנה השמור הוא מקור לגיטימי בפני
  עצמו, ובחירת קובץ חדש מחליפה אותו על הכונן.

בדיקות חדשות: עריכה שומרת את השינויים ואינה נוגעת בקובץ שכבר על הכונן,
עריכה של תוכנה שאינה רשומה נדחית ואינה יוצרת אותה, ובכל כרטיס יש כפתור
עריכה.

## Unreleased — תוסף כבד כבר לא נכשל ב-timeout בחיבור איטי

בסנכרון החנות דיווחו שני התוספים הגדולים ("פרפראות לתנ״ך", "עינים למקרא")
`TimeoutException after 0:00:20` וירדו מהמסך כאזהרה, בזמן שכל השאר עברו.

- **הזמן הקצוב היה על משך ההורדה כולה, לא על תקיעה.** `downloadAsset` משך את
  הנכס בקריאת `get()` אחת עם `.timeout(20s)` — ודדליין יחיד על גוף שגודלו
  לא ידוע פירושו "תסיים להוריד עשרות MB תוך 20 שניות", דרישה שכל חיבור ביתי
  איטי נכשל בה גם כשההורדה התקדמה יפה כל הזמן. עכשיו הבקשה נשלחת ב-`send()`,
  `timeout` חל על הכותרות בלבד, ו-`stallTimeout` חדש (30 שניות, כמו
  ב-`PatchDownloader`) חל על הזרם ומתאפס בכל מנת בייטים.
- **הגוף נכתב בזרימה ולא נצבר ב-RAM** — תוסף של עשרות MB כבר לא יושב כולו
  בזיכרון לפני שהוא נוגע בדיסק.
- **הורדה שנקטעה אינה משאירה קובץ חלקי.** הכתיבה הולכת ל-`.part` ומחליפה את
  היעד רק אחרי סגירה מוצלחת; הקובץ הקודם שבמראה נשאר שלם, ובכשל ה-`.part`
  נמחק. בלי זה, מעבר לזרימה היה מייצר קובץ קטום שנראה כתוסף תקין.
- תשובת שגיאה (סטטוס שאינו 200) ננטשת מיד בלי לרוקן את הזרם.
- **ההודעה למשתמש אינה עוד חריגה גולמית.** "לא ניתן להוריד את קובץ התוסף
  פרפראות לתנ״ך: TimeoutException after 0:00:20.000000: Future not completed"
  אינו אומר דבר למי שאינו מפתח. `PluginStoreClient.describeError` מתרגמת
  `TimeoutException` ל-`networkTimedOut` ("האתר לא השיב בזמן"), והיא עוברת
  בכל חמשת דיווחי הכשל של הסנכרון (מבנה, קטגוריה, תמונה, צילום מסך, קובץ)
  וגם ב-`siteUnreachable`. שאר סוגי השגיאות ממשיכים להיאמר כמות שהם.

בדיקות חדשות: הורדה שמתקדמת לאט עוברת גם כשמשכה הכולל ארוך מהקצוב, הורדה
שנתקעת נחתכת בלי `.part` ובלי לפגוע בקובץ שכבר היה שם, וכשל בזמן קצוב מדווח
בסנכרון בעברית.

## Unreleased — יעד התקנת הספרייה לא נגרר ממחשב אחר (issue #23)

במחשב שחשבון המשתמש שלו אינו `user` נכשלה התקנת הספרייה בשגיאת גישה, והמשתמש
נאלץ לבחור מיקום ידנית: התוכנה כיוונה את ההתקנה אל
`C:\Users\user\AppData\Roaming\otzaria\books` — התיקייה של **מחשב אחר**.

- **`library_state.json` יושב על הכונן, ולכן נתיב מוחלט שנשמר בו הוא פר-מחשב.**
  הרשומה נכתבת אחרי כל התקנה טרייה (`applyUpdate`) וגם בבחירה ידנית, והייתה
  רשומה גלובלית אחת: הכונן שהותקנה ממנו ספרייה במחשב הראשון הגיע למחשב השני
  ונשא איתו את נתיב הפרופיל שלו. תחת `C:\Users` משתמש רגיל אינו יכול ליצור
  תיקייה, ולכן `applyFullDownload` נכשל שם בשגיאת הרשאות במקום להתקין.
  `LibraryStateStore` שומר עכשיו ב-`customDbPaths` לפי `currentMachineKey()` —
  שם המחשב **ושם החשבון**, כי שני חשבונות באותו מחשב הם שני `%APPDATA%`.
- **הבחירה של כל מחשב נשמרת בנפרד ולא נדרסת** — כונן שנוסע בין שני מחשבים
  שבשניהם נבחר מיקום לא-שגרתי זוכר את שניהם.
- **רשומה מגרסה קודמת (בלי מזהה מחשב) נחשבת רק אם היא שימושית כאן:** נתיב מוחלט
  בפלטפורמה הנוכחית שתיקיית האם שלו קיימת. זה מה שמתקן את הכוננים שכבר בשטח,
  בלי להפסיד את הבחירה של המחשב שבו היא נכתבה.

בדיקות חדשות: חמש ב-`library_state_store_test.dart` (שמירה לפי מחשב, רשומה של
מחשב אחר, שמירה שאינה דורסת אותה, רשומה ישנה קיימת/חסרה, נתיב זר לפלטפורמה),
ואחת ב-`library_db_locator_test.dart` — יעד ההתקנה מתעלם מבחירה שהגיעה עם הכונן.

## Unreleased — עדכוני תוספים נראים בבדיקת הרשת, והסנכרון מוריד רק מה שהשתנה

תוסף חדש בחנות, או גרסה חדשה לתוסף שכבר במראה, לא הופיעו בשום מקום: הבדיקה
הקלה "יש עדכון ברשת?" שאלה על התוכנה, הספרייה והלאנצ'ר בלבד, והלאנצ'ר הכריז
"אין עדכונים חדשים ברשת" בזמן שבחנות היו כאלה.

- **`PluginsManager.peekOnlineUpdates()`** — קריאת מטא-דאטה אחת ל-`/api/plugins`
  מול `catalog.json` שבמראה, בלי להוריד נכס ובלי לכתוב דבר. `AppShell.checkOnline()`
  קורא לה יחד עם שאר המודולים, וכשל (אין רשת) נבלע כמו אצלם.
- **הבדיקה מסתכלת גם בדיסק, לא רק בקטלוג.** קובץ `.otzplugin` שנמחק, כונן
  שהועתק חלקית או הורדה שנכשלה משאירים רשומה מלאה בקטלוג בלי קובץ — והבדיקה
  ענתה "הכול מעודכן" על תיקייה שחסרים בה תוספים. עכשיו הם מדווחים כחסרים.
- **דף הבית מפרט מה נמצא**: "N תוספים חדשים בחנות" / "N תוספים עודכנו בחנות" /
  "N תוספים חסרים בתיקייה" — לתוספים אין מספר גרסה אחד להציג, ובלי הפירוט
  "יש עדכונים" לא סיפר מי התחדש.
- **הסנכרון מתוכנן לפני שהוא רץ, והתצוגה אומרת את האמת.** קודם נקבע לכל תוסף
  מה חסר לו במראה (בלי רשת), ורק מי שחסר לו משהו נכנס ללולאה: המונה מראה
  "1 מתוך 1" ולא "1 מתוך 40", שכבת הסנכרון אומרת במפורש שיורדים רק תוספים
  חדשים ומעודכנים, וההודעה בסיום היא "X ירדו, Y כבר היו מעודכנים" במקום
  "הסנכרון הושלם" סתמי. חנות שכולה מעודכנת מסתיימת בלי אף בקשה אחרי
  `/api/plugins`.
- **"הורד עכשיו" מריץ רק את הרכיבים שיש בהם מה להביא.** קודם הוא הריץ את
  שלושתם תמיד, כך שעדכון של שני תוספים גרר גם הורדת תוכנה וגם הורדת ספרייה —
  דקות של מדי התקדמות בלי שום תוצר, שנראו כמו "הוא מוריד הכול מחדש". רכיב
  מדולג רק כשהבדיקה הקלה **הוכיחה** שאין בו חדש (`provenUpToDateOnline`);
  בדיקה שלא רצה או שנכשלה אינה מוכיחה כלום ולעולם לא מדלגת, והספרייה אינה
  מדולגת כלל במצב "עדכון אישי". על מה שדולג נאמר בהודעה, לא בשקט.
- **קטלוג ישן לא גורר הורדה חוזרת של כל התמונות.** כתובת נכס ריקה ברשומה
  הקודמת פירושה "קטלוג שנכתב לפני שהשדה נוסף", לא "הכתובת השתנתה" — אחרת
  הסנכרון הראשון אחרי העדכון היה מוריד את כל תמונות החנות מחדש. ההכרעה שם
  נשענת על `updatedAt` ועל הקובץ שעל הדיסק, והשדה נכתב לקטלוג גם בלי הורדה.
- **`PluginSyncOutcome`** — `sync()` מחזיר עכשיו כמה ירדו, כמה דולגו ומה נכשל.
  כשקובץ תוסף לא ירד, תשובת "יש עדכונים ברשת" **נשארת** על המסך: המראה באמת
  עדיין חסרה אותו.
- **כשהורדת התוספים כבויה בהגדרות, דף הבית אומר זאת** מתחת למניין התוספים
  שנמצאו. בלי זה "הורד עכשיו" מדלג עליהם בשקט, והמשתמש רואה בדיוק את אותה
  הודעה גם אחרי ההורדה.
- **סנכרון מוריד רק את מה שהשתנה.** עד עכשיו רק קובץ ה-`.otzplugin` דולג
  (בגרסה זהה), ואילו התמונה וצילומי המסך של **כל** תוסף ירדו מחדש בכל סנכרון —
  בפועל הורדה חוזרת של החנות כולה בשביל תוסף אחד שהתעדכן. הקטלוג שומר עכשיו גם
  את הכתובות שמהן ירדו (`remoteImageUrl`, `remoteScreenshotUrls`), ונכס יורד רק
  כשהכתובת השתנתה, כש-`updatedAt` של התוסף זז, או כשהקובץ אינו על הדיסק. קובץ
  התוסף עצמו יורד גם כש-`downloadUrl` השתנה, לא רק כשמספר הגרסה זז.
  צילומי המסך הם קבוצה אחת (השמות לפי אינדקס), ומראה שסונכרנה לפני השינוי
  מורידה את התמונות פעם אחת נוספת — אין דרך לדעת אחרת מה כבר יש בה.

בדיקות חדשות: `plugin_online_peek_test.dart` (חדש/מעודכן/זהה, מראה ריקה, קובץ
שנמחק מהתיקייה, שההצצה אינה נוגעת במראה, ושהיא מסכימה עם מה שסנכרון מושך), שבע
בדיקות דילוג ותכנון ב-`plugin_mirror_sync_test.dart`, `checkOnline`
ב-`plugins_controller_test.dart` ושלוש בדיקות לכרטיס בדף הבית
ב-`screens_test.dart`.

## Unreleased — המסד מותקן למיקום של אוצריא, ובחירה אחרת מזהירה

התקנה ראשונה של הספרייה נחתה ב-`<dataDir>/library/seforim.db` — כלומר בתיקיית
הנתונים של הלאנצ'ר עצמו. בלאנצ'ר שרץ מכונן נייד זה אומר שהמסד (~5.5GB) הותקן
**על הכונן**: אוצריא שבמחשב לא ראתה אותו, והוא נסע משם ברגע שהכונן נשלף. אותו
תיקון כמו בהתקנת התוכנה עצמה (`resolveDefaultInstallDir`), הפעם למסד.

- **`LibraryDbLocator.resolveInstallDbPath()` קובע לאן תותקן ספרייה חדשה**:
  ההגדרה של אוצריא עצמה (`key-library-path`, גם כשהקובץ עוד לא קיים — שם היא
  תחפש), ואחרת ברירת המחדל של הפלטפורמה (`%APPDATA%\otzaria\books` בווינדוס).
  תיקיית הלאנצ'ר נשארה רשת ביטחון אחרונה בלבד, לפלטפורמה שאין בה מיקום ידוע.
  סדר החיפוש עצמו לא השתנה — הוא רוכז ל-`_otzariaDbCandidates` אחת, שממנה
  נגזרים גם האיתור, גם יעד ההתקנה וגם `isKnownToOtzaria`.
- **בחירת מיקום אחר אפשרית — אחרי אזהרה מפורשת.** כשאין עדיין מסד, מסך
  הספרייה מציג את **יעד ההתקנה** (במקום "לא נמצא") וכפתור "בחירת מיקום
  להתקנה". מיקום שאוצריא אינה מחפשת בו עובר דיאלוג שאומר את המחיר: יש לפתוח
  את אוצריא ולבחור שם את מיקום הספרייה, אחרת היא לא תראה את הספרים. אותה
  אזהרה חלה גם על "בחירת קובץ מסד".

בדיקות חדשות: `resolveInstallDbPath` ו-`isKnownToOtzaria` ב-
`library_db_locator_test.dart`, יעד ההתקנה של התקנה טרייה ב-
`library_manager_test.dart` (מפורשות **לא** תחת `dataDir`), ושתיים
ב-`screens_test.dart` על האריח והכפתור החדשים.

## Unreleased — חיווי התקדמות לעדכון המסד, גם בדף הבית

עדכון מסד אורך דקות ארוכות, אבל מי שהתחיל אותו **מדף הבית** ראה שם "מעדכן..."
בלבד לכל אורכו — ומתחתיו "אין פעולה זמינה". המד המלא (שלב, אחוז, בייטים) היה
קיים במסך הספרייה בלבד, כלומר במקום שהמשתמש לא בהכרח נמצא בו.

- **אריח הספרייה בדף הבית מציג את אותו מד** בזמן עדכון. שורת ההתקדמות אוחדה
  ל-`libraryApplyProgressRow` ומשמשת את שני המסכים, כדי שלא ייווצר שוב מצב
  שבו אחד מהם מפגר אחרי השני. "אין פעולה זמינה" נעלם כשהמד רץ — הוא התשובה
  לשאלה "מה קורה עכשיו".
- **שלב חילוץ המסד מדווח התקדמות אמיתית.** `ZstdFileDecompressor` מחזיר דרך
  `ReceivePort` כמה נקרא מהקובץ הדחוס מתוך גודלו; חילוץ של ~1GB היה עד עכשיו
  מד לא-קבוע לאורך דקות, השלב הארוך הבא אחרי אימות ה-hash. הבייטים הם של
  הקובץ הדחוס בכוונה — גודל המסד שייצא אינו ידוע מראש.
- `LibraryApplyProgress.bytesDownloaded` נקרא מעכשיו `bytesDone`: הוא נושא גם
  את בייטי החילוץ, ולא רק בייטים שירדו מהרשת.

בדיקות חדשות: אחת ב-`screens_test.dart` (האריח מציג שלב ואחוז, ובלי "אין
פעולה זמינה") ואחת ב-`zstd_file_decompressor_test.dart` (הדיווח עולה
מונוטונית ומגיע לסך הקובץ, מול libzstd אמיתי).

## Unreleased — עדכון אישי: קובצי עדכון בלבד, בלי המסד המלא

הצעה מהפורום (פוסט 33695): מי שהמסד כבר מותקן אצלו אינו צריך לסחוב אונקי עם
~1.5GB רק כדי לעדכן את עצמו. נוסף מתג **"עדכון אישי — למחשב שלי בלבד"**
(הגדרות → הורדה) שמצמצם את ההורדה לקובצי ה-patch מהגרסה שיש למשתמש ומעלה,
בלי המסד המלא — עשרות MB במקום כמה ג'יגה-בייט.

- **ברירת המחדל לא השתנתה, וההפעלה דורשת אישור.** התוכנה היא בראש ובראשונה
  כלי הפצה: כונן בלי המסד המלא אינו יכול להתקין ספרייה במחשב שאין בו אוצריא,
  וגם מסלול ההתאוששות (`fullDownloadFallback`, מ-issue #19) נעלם ממנו.
  הדיאלוג אומר את שני אלה לפני ההפעלה.
- **הגרסה נקראת רק בלחיצה** — "זהה את גרסת המסד שלי", במסך הספרייה. בדיקה
  שגרתית **אינה** רושמת אותה, וזה מכוון: הכונן מגיע גם למחשב המקוון, ואם
  *שם* מותקנת אוצריא חדשה יותר — קריאה אוטומטית שם הייתה מכוונת את ההורדה
  לגרסה שלו ומשאירה את המחשב הלא-מקוון בלי מסלול patches בכלל.
- **רשומה לכל מחשב, וההורדה יוצאת מהנמוכה שבהן** (`knownDbVersions` בקובץ
  ה-state, לפי שם מחשב + נתיב המסד). מי שלחץ בשני מחשבים מקבל הורדה שמשרתת
  את שניהם; מחשב שהתעדכן מרים את הרשומה שלו ומפסיק למשוך את המינימום למטה.
- **"אין גרסה חדשה" אינו מוחק את המראה.** `export` מחזיר `false` ואינו נוגע
  לא במניפסט ולא בניקוי הנכסים — אחרת מחשב מעודכן היה מוחק מראה תקינה שלמה.
- **מצב אישי בלי גרסה רשומה נופל להורדה הרגילה** ואומר זאת במסך, במקום
  להיכשל: מוטב מסד מלא מכונן שאין בו כלום.
- מסד שאינו על שרשרת ה-patches שירדה אינו מתעדכן בשקט אלא נחסם בנימוק
  (`blocked`) — המתכנן כבר ידע לחיות בלי מסד מלא במראה.

בדיקות חדשות: ארבע ב-`library_mirror_exporter_test.dart` (אין מסד מלא, שרשרת
דלתא מהמראה האישית, "אין חדש" שאינו מוחק, release עם מסד מלא בלבד), שש
ב-`library_state_store_test.dart` (מינימום, דריסה לפי מחשב, ערכים פגומים),
שלוש ב-`library_controller_test.dart` (בדיקה שגרתית אינה רושמת, לחיצה רושמת
ושורדת, מסד בלי גרסה) ושלוש ב-`screens_test.dart` (האריח, הדיווח, האזהרה).

## Unreleased — עדכון תוספים מתוך ההודעה עצמה

דיווח מהשטח על חנות התוספים: ההודעה "יש עדכונים זמינים" מפרטת מה מותקן
בגרסה ישנה, אבל לא היה בה שום דבר לעשות. כל שורה רק ניווטה לדף התוסף, לא
היה "עדכון הכל", וההודעה נפתחת פעם אחת בהרצה — מי שנכנס לתוסף אחד מתוכה
נשאר עם שבב "X עדכונים זמינים" בראש המסך ובלי שום דרך לדעת מי מהם עוד
ממתין. ארבעת החלקים תוקנו יחד:

- **כפתור "עדכון" בכל שורה** — מוסר את התוסף לאוצריא, אותה מסירה בדיוק
  שכפתור ההתקנה בכרטיס עושה. השורה נשארת ברשימה ומסומנת "נשלח לאוצריא".
- **"עדכון הכל"** מוסר את כל השורות הממתינות, בזו אחר זו. יש מרווח מכוון
  בין מסירות (`pluginUpdateDeliverySpacing`): כשאוצריא סגורה, הראשונה
  מפעילה אותה, ומסירה שנייה שמגיעה לפני שהמופע הראשון תפס את נעילת המופע
  היחיד הייתה פותחת מופע נוסף.
- **"נשלח" ו"עודכן" אינם אותו דבר.** ההתקנה עצמה מתבצעת אצל אוצריא, ולכן
  מסירה מוצלחת מסומנת "נשלח" בלבד; "עודכן" מוצג רק כשסריקת התוספים
  המותקנים מדווחת על הגרסה החדשה. כפתור "בדיקה מחדש" מריץ אותה, והדיאלוג
  מאזין לקונטרולר כך שהשורות משתנות תוך כדי. לסמן "עודכן" על סמך המסירה
  היה הופך את הרשימה למי-שמסרנו-לו במקום למי-שבאמת-מעודכן.
- **השבב בשורת הסנכרון הפך ללחיץ** ופותח את הרשימה מחדש, תמיד רק עם מה
  שעדיין ממתין — הפתיחה קוראת את הרשימה מהקונטרולר ולא מהתצלום שהוצג
  בהתחלה.

חמש בדיקות חדשות ב-`plugin_store_test.dart` נועלות את זה: המסירה בשורה
בודדת, הסדר והמרווח ב"עדכון הכל", מסירה שנכשלה שאינה מסומנת כנשלחה,
פתיחה מחדש מהשבב, וסריקה **אמיתית** (התקנה ניידת מומצאת בתיקייה זמנית)
שהופכת שורה שנשלחה ל"עודכן".

## Unreleased — הקובץ שמפורסם מקבל שם לטיני

הגרסה הראשונה שפורסמה אוטומטית (`v0.1.1`) העלתה את הקובץ לווינדוס בשם
**`default.exe`**. הסיבה: גיטהאב מנקה תווים שאינם ASCII משמות של נכסי
release, ו-`עדכוני אוצריא.exe` הוא שם שכולו עברית — אחרי הניקוי לא נשאר
ממנו דבר, וגיטהאב נופל לשם ברירת מחדל. אותו דבר בדיוק קרה ב-release הידני
`V1`, שנושא `default.exe` מאותה סיבה.

- ג'וב הפרסום מעלה מעכשיו את הקובץ כ-**`Otzaria-Updates.exe`**. השם שעל
  הכונן נשאר עברי, וגם השם המקומי של מי שכבר מותקן אצלו: העדכון העצמי מחליף
  את ה-stub *במקומו* ואינו משנה את שמו. השם הלטיני נוגע להורדה הידנית
  הראשונה — והוא זה שנדבק לה.
- **ולכן הלאנצ'ר חייב להכיר אותו בשני מקומות**, ושניהם תוקנו יחד עם השינוי:
  השם מכיל "otzaria", ובלי פסילה מפורשת כלל "התאמת שם מנצחת" ב-
  `OtzariaAppLocator` היה מאמץ את הלאנצ'ר כהתקנת אוצריא, קורא את הגרסה שלו
  ומריץ אותו במקומה — המוקש שמתועד ב-AGENTS.md §5, בשם חדש. בנוסף הוא נוסף
  לשוברי התיקו של `LauncherInstallLayout`, אחרת מי שהוריד ידנית היה מאבד את
  העדכון העצמי כשיש עוד exe באותה תיקייה.
- שלוש בדיקות חדשות מצמידות את זה: שג'וב הפרסום אכן מעלה בשם הזה, שהאיתור
  פוסל את שני שמות הלאנצ'ר (דרך `OtzariaAppLocator.isOurOwnExe`, שנחשף לשם
  כך — אין בין החבילות תלות בזמן קומפילציה), ושתיקייה שיש בה רק את הקובץ
  הזה אינה מזוהה כהתקנה של אוצריא. וג'וב הפרסום נכשל ברעש אם לא נמצא exe
  בארטיפקטים, במקום לפרסם release בלי הקובץ לווינדוס.

## Unreleased — תיקוני באגים מסבב בדיקות

סבב הרצה של כל חבילות הפרויקט וסקירה של השינויים שטרם נדחפו. כל הבדיקות
עברו; מה שנמצא לא נמצא על ידיהן אלא בקריאת הקוד, ולכן כל תיקון כאן קיבל
בדיקה שתתפוס אותו בפעם הבאה.

- **המסד שנבחר ידנית שוב מציג את גרסתו.** משתמש באוצריא ניידת דיווח שאחרי
  בחירת `seforim.db` ידנית התוכנה "לא מזהה את גרסת המסד". השורש:
  `LibraryManager.checkForUpdate` קורא את הגרסה מהמסד, וממשיך משם למראה —
  וכשאין מראה עדיין הוא זורק `LibraryMirrorMissingException`, מה שהשליך גם
  את הגרסה שכבר נקראה. המסך הציג "לא ידוע" למסד שנמצא ונקרא בהצלחה.
  `LibraryManager.readLocalVersion()` קורא אותה בלי לגעת במראה, והקונטרולר
  משתמש בה במסלול "צריך להוריד". מסד בלי `schema_meta` נשאר "לא ידוע" —
  ולא "0".
- **דליפת זיכרון נייטיבי ב-`FastSha256`.** ה-sink הנייטיבי מקצה חוצץ של
  1MB ואובייקט hash מחוץ לאיסוף האשפה של דארט, ושחרר אותם רק ב-`close()`.
  כל ביטול הורדה, כשל רשת או שגיאת SQLite באמצע חישוב דילגו עליו — כלומר
  ~1MB לכל ביטול של נכס בן 1.5GB, עד סוף ההרצה. `FastSha256.start` מחזיר
  מעכשיו `FastSha256Sink` עם `dispose()`, וכל הקוראים משחררים ב-`finally`.
  בנוסף, `add` אחרי `close` זורק במקום לכתוב לזיכרון משוחרר (שחיתות heap
  שקטה) — בדיוק כמו `package:crypto`.
- **כשל התקנת עדכון עצמי הסתיר את כפתור הניסיון החוזר.** המצב עבר ל-`error`,
  ואיתו נעלם הכפתור, אף שהקובץ עדיין במראה — במחשב מנותק זו הפעולה היחידה
  שנשארה, ורק הפעלה מחדש של הלאנצ'ר החזירה אותה. עכשיו המצב חוזר ל"מוכן
  להתקנה" וההודעה מוצגת לצד הכפתור.
- **בדיקה שהייתה נהיית אדומה מעצמה.** `self_update_test.dart` השווה מול תג
  קבוע `v0.2.0`, בזמן שה-CI מקדם את `launcherVersion` בכל דחיפה ל-main —
  ביום שהגרסה הייתה משיגה אותו, הבדיקה נכשלה בג'וב שמפרסם. התג נגזר מעכשיו
  מהגרסה הרצה.
- שני מללים ב-`otzaria_l10n` שאיש לא קרא להם (`noAssetForRelease`,
  `seedColorResetPrompt`) הוסרו, ו-`AGENTS.md` עודכן בשלוש נקודות שהקוד
  כבר סתר.

## Unreleased — עדכון המסד לוקח פחות זמן

משתמש דיווח שעדכון המסד נמשך זמן רב. הפירוק הראה שהחלק הכבד אינו ההחלה
עצמה אלא **אימות ה-hash הלוגי שאחריה**: `LogicalContentHasher.compute`
קורא את כל תוכן המסד, מקדד אותו לזרם בתים ומריץ עליו SHA-256. נמדד על מסד
אמיתי של 7.4GB (5,896MB תוכן לוגי, `db_version=20`) — ושני דברים יצאו
משם. התוכנית המלאה, כולל מה שטרם מומש, ב-`DB_UPDATE_SPEED_PLAN.md`.

**1. ה-SHA-256 עבר לספריית ההצפנה של המערכת.** המימוש של `package:crypto`
הוא דארט טהור; במיקרו-בנצ'מרק הוא נתן 50MB/s מול 1,225MB/s לאותו אלגוריתם
דרך CNG של Windows. על הצינור המלא, שבו קריאת ה-SQLite נהפכת לחוסם, המדידה
על המסד האמיתי (ארבעה סבבים מתחלפים, כדי לנטרל את מטמון הדיסק) נתנה
**66MB/s מול 22MB/s — פי 3**.

- **`FastSha256`** (חבילת השורש) מחליף את `sha256.startChunkedConversion`
  בכל מקום שבו הוא היה: `LogicalContentHasher` ואימותי הנכסים ב-
  `PatchDownloader` (שם מדובר בקובץ של ~1.5GB — ה"דקה ויותר" שכבר תועד
  כאן). Windows דרך CNG, macOS דרך CommonCrypto ב-libSystem, ולינוקס
  נופלת ל-`package:crypto` בכוונה: אין שם ספריית מערכת מובטחת, וזו
  אופטימיזציה ולא דרישה.
- **חוזה ה-hash מול ה-Kotlin לא נגע.** זרם הבתים שנכנס ל-SHA לא השתנה
  בכלל — רק המימוש שמתחתיו, ושני המסלולים החזירו את אותו hash על 5,896MB
  אמיתיים. מבחני ה-golden עברו ללא שינוי, ונוסף golden שמריץ את אותו מסד
  בשני המסלולים ומשווה, כך שרגרסיה במסלול אחד לא "תעבור" בפלטפורמה
  שמשתמשת בשני.
- **בדיקה עצמית לפני שימוש.** מימוש נייטיבי שגוי היה דוחה *כל* עדכון,
  ולכן בטעינה הראשונה (פעם אחת לכל isolate) `FastSha256` מוודא שהוא מסכים
  עם `package:crypto` על שלושה קלטים — ריק, בתי UTF-8 עבריים גולמיים עם
  BOM, וגוש שחוצה בלוקים — ומשתיק את המסלול הנייטיבי אם לא. ספרייה חסרה,
  סמל חסר או פלטפורמה לא מוכרת נופלים לאותו מסלול בשקט.

**2. אימות ה-hash רץ פעם אחת, בסוף השרשרת.** ה-hash הוא על תוכן ה-DB כולו,
ולכן התאמה ל-`toContentHash` של הצעד האחרון מוכיחה את **כל** השרשרת. עד
עכשיו הוא רץ בכל צעד: מי שפספס חמש גרסאות שילם חמש קריאות מלאות של המסד.
החיסכון הוא פי מספר ה-patches בשרשרת (עד 9 במראה של עשרה releases).

- `PatchApplier.apply` קיבל `verifyToHash` (ברירת מחדל **פעיל**, כדי
  שהמנוע יישאר תואם לאוצריא), ו-`LibraryUpdateApplier.applyDelta` — שכבת
  התזמור שלנו — מכבה אותו בכל צעד שאינו האחרון.
- **מה שמונע מסד לא-מאומת בשקט:** שרשרת שנקטעה באמצע (ביטול, כשל הורדה)
  משאירה מסד שהוחל נקי אך לא אומת. `LibraryDbRecoveryService.markUnverified`
  רושם את הגרסה הזו ב-`<db>.unverified`, וההחלה הבאה שמתחילה מאותה גרסה
  מפעילה `verifyFromHash` ומאמתת את המסד **לפני** שהיא בונה עליו. תוכן
  שגוי נדחה שם עם ההודעה שכבר קיימת ("נדרשת הורדה מלאה"). הסימון נמחק אחרי
  אימות שהצליח, ואחרי התקנת מסד מלא (שממילא מאומת ב-`quick_check` וגרסה).
- לשרשרת של צעד אחד — הרוב המכריע של המקרים — אין שום שינוי בהתנהגות.
- מד ההתקדמות מדווח מעכשיו את תת-השלב הנכון בזמן חישוב hash: ייתכן גם
  `verifyFromHash`, ולא רק `verifyToHash`.

## Unreleased — אינדקס החיפוש של אוצריא מתעדכן אחרי עדכון מסד

הפער שהיה פתוח כאן מההתחלה: הלאנצ'ר מחליף את `seforim.db` מבחוץ, ואוצריא
לא יודעת שהתוכן התחלף — `isBookIndexed` בודק נוכחות מפתח ולא תוכן, ולכן
חיפוש בספר שהתעדכן החזיר את הגרסה הישנה עד אינדוקס ידני. ביקשנו מצוות
אוצריא לסגור אותו
([issue #734](https://github.com/Otzaria/otzaria/issues/734#issuecomment-5251000659)),
והם מימשו זאת אחרת ממה שהצענו: לא קריאה של `.otzaria-external-update.json`
אלא **קישור עומק** — `otzaria://library/reindex` (ענף `dev`, `78f395f3`).
הוא מרענן את הקטלוג מהדיסק ומריץ `StartIndexing` + `ReconcileIndex`, גם
כשעדכון האינדקס האוטומטי שלהם כבוי, ומזהה לבד אילו ספרים השתנו (השוואת
טביעות-אצבע) — כך שרשימת `booksTouched` שלנו אינה נדרשת לו.

- **הבקשה נמסרת לאוצריא, לא נכתבת לה.** `OtzariaManager.requestLibraryReindex()`
  מוסר את הקישור **לקובץ ההרצה שזוהה** כארגומנט, ולא דרך מטפל הפרוטוקול של
  מערכת ההפעלה — בדיוק כמו התקנת תוסף, ומאותו טעם: התקנה ניידת אינה רושמת
  את הסכימה `otzaria://`. מופע פתוח מקבל את הבקשה דרך ה-single-instance שלו.
- **שתי דרכי מסירה.** דיאלוג מוצע מיד אחרי עדכון מסד (בדף הבית ובמסך
  הספרייה), ואריח קבוע בכרטיס מצב המסד למי שדחה אותו. ובנוסף, **כל הפעלה
  רגילה של אוצריא מהלאנצ'ר** מוסרת בקשה שממתינה — את אוצריא המשתמש פותח
  ממילא.
- **`.otzaria-external-update.json` הפך לסימון שלנו.** הוא נשאר נכתב בסוף
  כל עדכון מוצלח, אבל תפקידו עכשיו הוא לזכור שבקשה ממתינה: הוא שורד הפעלות
  מחדש של הלאנצ'ר ונמחק **רק** אחרי מסירה שהצליחה. הפעלה שנכשלה, או סירוב
  בדיאלוג, משאירים אותו — ההצעה תחזור.
- **מה עדיין פתוח:** מי שפותח את אוצריא לא דרך הלאנצ'ר לא מוסר את הבקשה,
  והסימון ממתין להזדמנות הבאה. זה המחיר של פתרון מבוסס-קישור, ומתועד
  ב-`library_manager/README.md`.

## Unreleased — תוספים לאוצריא ניידת

דיווח משתמש: אחרי שהלאנצ'ר כבר זיהה אוצריא **ניידת** (לפי התהליך הרץ או
לפי בחירה ידנית של התיקייה), החנות עדיין הציגה "אין תוספים מותקנים",
וההתקנה הישירה נכשלה ב"פתיחת אוצריא נכשלה. ודא שאוצריא מותקנת במחשב זה".
שתי הסיבות היו אותה סיבה: מודול התוספים לא הכיר את נתיב ההתקנה שכל שאר
הלאנצ'ר כבר ידע.

- **תיקיית התוספים נגזרת מנתיב ההתקנה שזוהה**, בדיוק כמו איתור
  ה-`seforim.db`: `<exeDir>/otzaria_data/plugins` כשיש `portable.marker`
  ליד קובץ ההרצה (או כשתיקיית הנתונים כבר שם), ורק אחרת
  `%APPDATA%\otzaria\plugins` — ואחריה ההתקנה המערכתית. התקנה ניידת אינה
  משתמשת ב-`%APPDATA%` בכלל, ולכן הסריקה שם החזירה מפה ריקה.
- **ההתקנה הישירה נמסרת לקובץ ההרצה עצמו**: אותו `otzaria://plugin/install-local`
  בדיוק, אבל כארגומנט לאוצריא שזוהתה — מה שהרישום ברג'יסטרי היה עושה
  (`"otzaria.exe" "%1"`). התקנה ניידת אינה רושמת את הסכימה `otzaria://`,
  ולכן המסלול דרך מערכת ההפעלה נכשל אצלה תמיד. בלי נתיב ידוע, או כשהוא
  כבר לא קיים, נופלים חזרה למטפל הפרוטוקול כמו קודם.
- **סריקה חוזרת אחרי שהנתיב מתברר** — נתיב ההתקנה ידוע רק אחרי בדיקת
  התוכנה, שרצה אחרי טעינת החנות. `AppShell.checkAll()` והבחירה הידנית של
  תיקיית ההתקנה מריצות עכשיו `PluginsModuleController.refreshInstalled()`,
  שסורק רק את המותקנים ואינו מחזיר את המסך למצב טעינה.
- סימון ההתקנה הניידת ושם תיקיית הנתונים מוגדרים בשתי חבילות
  (`library_manager` ו-`plugins_manager`), ובדיקה חדשה בלאנצ'ר
  (`test/portable_paths_test.dart`) מוודאת שהם לא ייפרדו — כמו
  `process_names_test.dart`.

## Unreleased — patch שנכשל אינו מסלול ללא מוצא

מאותו דיווח ([issue #19](https://github.com/Otzaria/Otzaria_Offline_update/issues/19)):
עדכון מגרסה 18 ל-20 נפל בשני מחשבים על
`SqliteException(2067): UNIQUE constraint failed: tocText.text` — הודעה
אטומה שאין ממנה דרך להתקדם. הכשל עצמו אינו שלנו: ל-`tocText.text` יש
`UNIQUE` מעל ה-PK, ה-`ON CONFLICT` מכוון ל-PK בלבד, וה-upserts רצים לפני
ה-deletes — כך שערך שעבר בין id-ים מתפוצץ. המנוע כאן זהה אות-באות
ל-`otzaria_library_updater` ול-`PatchApplier.kt` שבצד המפיק, ולכן אותו patch
יפיל גם את העדכון המקוון של אוצריא. מה שכן שלנו: מה המשתמש רואה, ומה הוא
יכול לעשות.

- **הודעה שאומרת מה קרה ומה לעשות** במקום ה-`SqliteException` הגולמי:
  `PatchApplier` מזהה הפרת אילוץ בשלב ה-upserts, מציין את שם הטבלה, ומוסיף
  שהמסד לא נגע ושנדרשת הורדה מלאה. פרטי ה-SQLite נשמרים בסוף ההודעה.
- **מסלול יציאה**: תוכנית דלתא נושאת מעכשיו את ההורדה המלאה כמסלול חלופי
  (`LibraryUpdatePlan.fullDownloadFallback`) — המסד המלא ממילא יושב במראה.
  אחרי כשל, מסך הספרייה מציג "התקנת הספרייה המלאה", עם דיאלוג שאומר את
  הגודל ושזו פעולה ארוכה.
- **אינו אוטומטי בכוונה.** הורדה של ~1.5GB וחילוץ של ~5.5GB היא החלטה של
  המשתמש, לא החלפת מסלול שקטה מאחורי גבו — באותה רוח שבה אין כאן נפילה
  שקטה לרשת.

## Unreleased — "אוצריא מעודכנת" במחשב שאין בו אוצריא

דיווח משתמש ([issue #19](https://github.com/Otzaria/Otzaria_Offline_update/issues/19)):
הלאנצ'ר סירב להתקין את אוצריא בשני מחשבים שלא הייתה בהם כלל, והודיע
שהתוכנה מעודכנת. הסיבה: `otzaria_install_state.json` יושב ב-`OtzariaData`
שעל הכונן הנייד, ולכן ההתקנה שנרשמה במחשב אחד נסעה לכל המחשבים הבאים —
ו-`checkForUpdate` קיבלה אותה כאמת בלי לבדוק כלום.

- **ה-state מאומת מול הדיסק של המחשב הנוכחי** לפני שמסתמכים עליו
  (`OtzariaManager._verifyStoredState`): אם נתיב ההפעלה שהוא מתאר אינו שם,
  הוא נפסל וממשיכים לזיהוי הרגיל (תהליך רץ → רג'יסטרי → תיקיות ברירת
  המחדל). אותו אימות חל גם על "הפעלה", שקודם ניסתה להריץ קובץ שאינו קיים.
- **הקובץ אינו נמחק** — הוא עשוי להיות תקף לחלוטין במחשב שהכונן יחזור
  אליו. הוא פשוט חדל להיות מקור אמת.
- **גם הגרסה נקראת מחדש מקובץ ההרצה** ונכתבת בחזרה כשהיא שונה, כך שהתקנה
  שהמשתמש עדכן במתקין של אוצריא עצמה אינה מוצגת בגרסה שאנחנו "זוכרים".
  כשל קריאה משאיר את התג השמור.

## Unreleased — התוכנה מעדכנת את עצמה, ושומרת על הנתונים

הלאנצ'ר ידע לעדכן את אוצריא, את הספרייה ואת התוספים — ולא את עצמו. גרסה
חדשה שלו הגיעה למשתמשים בדרך אחת: להוריד exe מהאתר ולהחליף אותו ביד.
עכשיו הוא מציע זאת בעצמו, ומחליף **רק את התוכנה**: ההגדרות, המראות
וההתקנה המנוהלת של אוצריא נשארות כמו שהן.

- **`lib/src/self_update/`** — אותה חלוקה כמו כל מודול אחר: הורדה אל
  `mirror/launcher/` היא הפעולה היחידה שנוגעת ברשת, והבדיקה וההתקנה
  קוראות רק מהתיקייה שלצד התוכנה. כך גם העדכון של התוכנה נוסע על הכונן:
  מורידים במחשב המקוון, ומתקינים בכל מחשב — כולל המנותק.
- **"גירסה X של עדכוני אוצריא זמינה, להוריד עכשיו?"** קופץ בפתיחה אחרי
  הבדיקה הקלה ברשת (פעם אחת בהרצה, ורק אם `autoCheckOnlineUpdates`
  דלוק), ובסיום ההורדה — "להתקין ולהפעיל מחדש?". שתי הפעולות מחכות גם
  בכרטיס שבדף הבית, שמופיע רק כשיש בו מה לומר; מספר הגרסה עצמו נוסף
  לכרטיס התמיכה בהגדרות.
- **מוחלף ה-stub, לא `launcher_app.exe`.** ה-stub אינו רץ (הוא מסיים מיד
  אחרי `CreateProcessW`), ולכן אפשר לדרוס אותו מתוך התוכנה; הקובץ שבתוך
  `app-files` נעול כל זמן הריצה. ההחלפה היא שתי החלפות שם עם גלגול
  אחורה, בדיוק כמו החלפת `seforim.db`.
- **`.ready` מחזיק עכשיו את גרסת ה-payload.** זה מה שהופך exe מוחלף
  לחילוץ מחדש: הגרסה שבמרקר אינה תואמת, ה-stub ממתין לסגירת התהליך הישן
  (`--after-update=<pid>` — אחרת `tar` נכשל על ה-DLL הנעולה) ומחלץ **מעל**
  `app-files`, בלי למחוק דבר. `OtzariaData/` שיושבת בתוכה לא נגעת.
- **`OTZARIA_LAUNCHER_STUB`**: ה-stub מוסר ללאנצ'ר את הנתיב של עצמו, כי
  `Platform.resolvedExecutable` מצביע לתוך `app-files`. לגרסאות שעוד לא
  הציבו אותו יש גיבוי — ה-exe הבודד שליד `app-files` — ולכן גם העדכון
  הראשון אפשרי. `stub_contract_test.dart` מצמיד את כל השמות המשותפים
  בין `stub.c`, `package.ps1` וה-Dart.
- **גרסה ממקור אחד**: `build_stub.ps1` מייצר `version.h` מ-`pubspec.yaml`,
  וממנו מקבלים את הגרסה גם המרקר וגם משאב הגרסה של ה-exe (שהיה קבוע על
  `1.0.0`).
- **פרסום אוטומטי, בתוך `ci.yml`**: דחיפה ל-`main` שכל הבדיקות בה עברו
  מקדמת את ה-patch, כותבת אותו לקוד
  ([`tool/set_launcher_version.sh`](tool/set_launcher_version.sh)), מתייגת,
  ומעלה release עם ה-exe וה-zip — כלומר עם הארטיפקטים של ג'ובי הבדיקה
  עצמם, בלי בנייה שנייה. הפרסום יושב באותו workflow ולא לצידו בדיוק מהסיבה
  הזאת. מכאן הלאנצ'ר של המשתמשים מוצא את הגרסה בעצמו.
- **הריצה השבועית (`cron: "0 3 * * 1"`) בוטלה** — הבדיקות רצות על כל
  דחיפה, וזה הטריגר היחיד.
- **רק תג בצורת `vX.Y.Z` נחשב release, ונבחרת הגרסה הגבוהה** ולא הראשונה
  שהחזיר GitHub. הריפו נושא release ידני ותיק בשם `V1`, ו-`normalize` רואה
  בו גרסה 1 — כלומר חדשה לנצח מכל 0.x. בלי הפסילה הזאת המשתמשים היו
  מקבלים בסוף את `default.exe` הוותיק שתלוי שם.
- **ב-macOS** מוחלפת חבילת ה-`.app` כולה (`ditto`, לא `unzip`) ואין הפעלה
  מחדש אוטומטית — מוצגת בקשה לפתוח מחדש. לא אומת על חומרה.

## Unreleased — פלטת הצבעים של אוצריא בהגדרות

שכבת העיצוב פורטה מאוצריא כדי ששתי התוכנות ייראו זהות, אבל הפלטה עצמה
נשארה מחוץ להישג יד: `AppSeedColors` היה בקוד ולא היה מי שיקרא לו,
וה-seed היה קבוע — מי שצבע את אוצריא בטורקיז פגש לאנצ'ר חום.

- **`ColorPickerTile`** (`launcher_app/lib/src/widgets/color_picker_tile.dart`)
  — פורט מ-`otzaria/lib/settings/dialogs/color_picker_dialog.dart`: אותם
  שלושה־עשר גוונים באותו סדר, הצבע חל מיד בלחיצה, וכפתור איפוס מחזיר
  לברירת המחדל. יושב בכרטיס "שפה ומראה", אחרי בורר ערכת הנושא.
- **צבע לכל בהירות**: `ui.seedColor` ו-`ui.darkSeedColor` (schema 6),
  ARGB שלם כמו `key-swatch-color` באוצריא. הבחירה חלה על הערכה המוצגת
  באותו רגע — בדיוק כמו שם — ולכן שינוי בערכה הבהירה אינו נוגע בכהה.
- **שמות הגוונים עברו ל-`otzaria_l10n`**. באוצריא הרשימה נושאת את השם
  העברי עצמו; כאן היא מזוהה ב-`SeedColorLabel`, וה-`switch` בבורר ממצה
  עליו — צבע חדש בפלטה אינו מתקמפל עד שיקבל שם בעברית ובאנגלית. בעקבות
  זאת בוטלו שני הפטורים שהיו ל-`app_seed_colors.dart` בסורקי הליטרלים.

## Unreleased — שפת הממשק נקבעת לפי שפת המחשב

הלאנצ'ר נפתח תמיד בעברית, גם על מחשב שכל הממשק שלו באנגלית: `ui.language`
נשמר כקוד שפה בלבד (`he`/`en`), וברירת המחדל שלו הייתה עברית קשיחה. משתמש
שאינו קורא עברית פגש מסך שאינו יכול לקרוא, בלי לדעת שיש בו בורר שפה.

- **`AppLanguagePreference`** (`launcher_app/lib/src/settings/app_settings.dart`)
  מפריד בין ה*בחירה* לשפה שמוצגת: `system` (ברירת המחדל), `hebrew`,
  `english`. `system` נפתר בכל קריאה מ-`PlatformDispatcher.instance.locales`,
  ולכן החלפת שפה במערכת ההפעלה נתפסת מעצמה בהרצה הבאה.
- **`AppLanguage.forLanguageCode`** ממפה locale של מערכת ההפעלה (`he-IL`,
  `en_US.UTF-8`, וגם `iw` — הקוד הישן לעברית) לשפה שהלאנצ'ר מדבר. שפה שאיננו
  מדברים נופלת לאנגלית, בשונה מ-`fromCode` שקורא ערך שמור ונופל לעברית.
- **"אוטומטי" הוא אפשרות שלישית בבורר השפה** שבהגדרות, ולא ברירת מחדל
  נסתרת — מי שבחר שפה מפורשת נשאר בה, גם כשהמחשב מדבר אחרת. קובץ הגדרות
  ישן שכבר נושא `he`/`en` מכובד כבחירה מפורשת (schema 5).
- **`main` מציב את שפת המחשב לפני `AppPaths.resolve()`**: הודעת כשל באתחול
  נוסחת מ-`AppL10n` ברגע הזריקה, ולכן מסך "אין לאן לכתוב" היה מוצג בעברית
  גם באנגלית. `SettingsController.load` מציב עכשיו את השפה גם כשאין קובץ
  הגדרות, אחרת מלל התשתית נשאר עברית בזמן שהמסכים באנגלית.

## Unreleased — פתיחת תוסף מהודעת העדכונים מעבירה גם את המסך

הודעת "יש עדכונים לתוספים" נפתחת מתוך מסך חנות התוספים, אבל היא נפתחת מעל
כל האפליקציה — והמסך עצמו נשאר בעץ גם אחרי שהמשתמש עבר ממנו לדף הבית (זו
בדיוק המטרה של הבנייה המדורגת ב-`AppShell`). לכן סנכרון שהתחיל מדף הבית
הקפיץ את ההודעה שם, ולחיצה על תוסף פתחה את עמוד הפרטים שלו *מאחורי* דף
הבית: הניווט נשאר על "בית", והמשתמש ראה שלחיצתו לא עשתה כלום.

- **`PluginsScreen.onRequestFocus`**: המסך מבקש מהמסגרת להעביר אליו את
  התצוגה כשנבחר תוסף מהודעת העדכונים, ו-`AppShell` מחבר את הבקשה ל-`_goTo`
  של לשונית התוספים. הפרטים נפתחים עכשיו במסך שרואים.

## Unreleased — הורדת הספרייה ירדה מ-~7.5GB ל-~1.6GB

הורדת עדכוני הספרייה משכה את המסד המלא (`seforim.db.zst`, ~1.5GB) **מכל
אחד** מחמשת ה-releases שבחלון ההיסטוריה — v20, v19, v18, v17, v16 — כלומר
כ-7.5GB, מתוכם כ-6GB שלא ייקראו לעולם. `LibraryUpdateDiscovery` בוחרת
באופליין רק את המסד המלא של הגרסה הגבוהה ביותר; ארבעת העותקים האחרים היו
מקום מבוזבז על הכונן הנייד ושעות הורדה. מטרת חלון חמשת ה-releases היא
ה-**patches** (עשרות MB כל אחד), לא המסד המלא.

- **המסד המלא יורד פעם אחת**, מה-release הגבוה ביותר שנושא אותו
  (`LibraryMirrorExporter._fullDbCarrier`) — בדיוק הנכס שהמסלול המלא
  באופליין בוחר. ה-patches של כל חמשת ה-releases ממשיכים לירד כרגיל, ולכן
  מסלול הדלתא לא נפגע.
- **נכסים נטושים נמחקים** בסוף הורדה מוצלחת: תיקיית `release` שנפלה מחלון
  ההיסטוריה, וכל קובץ שאינו במניפסט החדש. עד עכשיו הם נשארו על הכונן לעד,
  כך שגם אחרי התיקון הזה מראה קיימת הייתה נשארת עם העותקים המיותרים.
  קובצי ה-`.resume` של נכסים שכן במניפסט נשמרים — בלעדיהם הריצה הבאה מוחקת
  נכס שלם ומורידה 1.5GB מחדש.

## Unreleased — "הפעל" עובד גם על התקנה שהלאנצ'ר לא התקין

הלאנצ'ר זיהה נכון התקנה קיימת (דרך רישומי ההסרה או תיקיית ברירת המחדל),
הציג את הגרסה שלה, והפעיל את כפתור "הפעל" — ובלחיצה ענה "אוצריא עדיין לא
הותקנה על ידי הלאנצ׳ר הזה". הסיבה: זיהוי וההפעלה קראו משני מקורות שונים.
`checkForUpdate()` שומר ב-state רק את הזיהוי לפי **התהליך הרץ** (תצפית
חולפת, שנעלמת ברגע שאוצריא נסגרת), ובכוונה לא את הזיהוי לפי תיקייה — הוא
חוזר בכל בדיקה ממילא, ושמירתו הייתה "אימוץ" שקט שקובע לאן ייכתבו עדכונים
עתידיים. `launch()`, לעומת זאת, קרא מה-state בלבד. לכן התופעה נראתה
מקרית: כשאוצריא הייתה פתוחה בעליית הלאנצ'ר ההפעלה עבדה, וכשהייתה סגורה —
לא.

- **`OtzariaManager.launch()` נופל חזרה לזיהוי במיקומים המוכרים** כשאין
  state שמור, ומפעיל את מה שנמצא. אין כאן שמירה — ההתנהגות של "לאן
  מתעדכנים" לא משתנה, רק ההפעלה.
- **הודעת הכשל מדויקת עכשיו**: היא נזרקת רק כשלא נמצאה שום התקנה, ומפנה
  להתקין או לבחור את תיקיית ההתקנה הקיימת (`noOtzariaInstallFound` במקום
  `notInstalledByThisLauncher`, שתיאר את הסיבה הלא-נכונה).
- **`OtzariaLauncher` ניתן להזרקה** לבנאי של `OtzariaManager`, כדי שאפשר
  יהיה לבדוק את הנתיב שנבחר להפעלה בלי להריץ תהליך אמיתי.

## Unreleased — סגירת אוצריא מזוהה בזמן שהלאנצ'ר פתוח

אוצריא שהייתה פתוחה בעליית הלאנצ'ר נשארה "פתוחה" מבחינתו גם אחרי שנסגרה:
האזהרה המשיכה להופיע, ולחיצה על "עדכן" ענתה "אוצריא פתוחה — יש לסגור
אותה" עד שהלאנצ'ר עצמו נסגר ונפתח מחדש. הסיבה הייתה **העתק** של מצב
התהליך ב-`AppShell` (שדה `_otzariaIsRunning`), שהתמלא בעלייה ולא רוענן
אחר כך — לא כשמסך התוכנה/הספרייה הריץ "בדוק שוב", ולא לפני העדכון עצמו.

- **מצב התהליך נגזר מ-`OtzariaModuleController.isRunning`** ולא מועתק, כך
  שכל בדיקה שמרעננת אותו (כולל "בדוק שוב" בשני המסכים) מעדכנת גם את
  התצוגה.
- **`OtzariaModuleController.refreshRunningState()`** מרענן את הבדיקה לבדה
  (בלי קריאת גרסאות ובלי רשת) ומחזיר את התוצאה. קוראים בו-זמנית מצטרפים
  לבדיקה שבאוויר — היא ~300ms — במקום להריץ `tasklist` שני. לפני פעולה
  חוסמת, לעומת זאת, `force: true` מחייב בדיקה שמתחילה *עכשיו*: הצטרפות
  מחזירה תמונה מלפני עד ~300ms, ובחלון הזה אוצריא יכולה להיפתח — כלומר
  עדכון מסד שירוץ תחתיה, בדיוק מה שהבדיקה נועדה למנוע.
- **כל עוד אוצריא פתוחה, הלאנצ'ר בודק שוב כל 3 שניות** וסגירה מתגלה
  מעצמה. כשהיא סגורה אין טיימר כלל. הבדיקה מודיעה **רק על שינוי** —
  הודעה בכל דגימה בנתה מחדש את כל עץ הווידג'טים כל 3 שניות.
- **גם פתיחה מזוהה, לא רק סגירה.** `launch()` מציב `isRunning` (ולא דוגם:
  התהליך עדיין לא בהכרח ב-`tasklist`), ומשם הרענון המחזורי מתקן אם לא עלה.
  בלי זה, אוצריא שנפתחה מהלאנצ'ר נשארה "סגורה" מבחינתו — התמונה המשלימה
  של אותו באג.
- **הבדיקה שלפני עדכון הספרייה משתמשת בערך המוחזר.** במסך הספרייה היא
  אמנם רוענן קודם, אבל התנאי נבדק מול השדה שנלכד בבנייה (המסכים הם
  `StatelessWidget`) — כלומר תמיד הערך הישן; בדף הבית לא הייתה בדיקה
  מחדש בכלל.
- **`LibraryModuleController.update()` חסום לכניסה חוזרת.** בין הלחיצה
  לדיאלוג יש עכשיו בדיקת תהליך אסינכרונית, והכפתור פעיל בזמנה — לחיצה
  כפולה הייתה מריצה שתי החלות במקביל על ה-DB החי.

### תיקוני זיהוי ההתקנה שנמצאו בסריקה של השינויים

- **הלאנצ'ר זיהה את עצמו כאוצריא.** ה-stub נקרא `עדכוני אוצריא.exe`, ושם
  תואם "מנצח מיד" — כך שתיקיית עדכונים שהועתקה אל תוך `C:\אוצריא` גרמה
  ללאנצ'ר לאמץ את עצמו, לדווח על הגרסה שלו, ו"להפעיל את אוצריא" = להפעיל
  את עצמו.
- **סריקת ה-exe בווינדוס עברה לרוחב, עם חסם עומק** — כמו זו של macOS.
  קודם התוצאה הייתה תלויה בסדר לא-מובטח של `Directory.list` (exe מקונן
  יכול היה לנצח את זה שבשורש), ו-`C:\אוצריא` — שהיא גם מיקום נפוץ של
  ספריית הספרים — נסרקה לעומק, ~1GB, בכל בדיקה.
- **הרג'יסטרי**: זיהוי לפי `DisplayName` בלבד (נתיב שמזכיר "otzaria" הכניס
  תוכנות זרות לזיהוי האוטומטי, בלי שום אימות נוסף); `UninstallString`
  מנותח כפקודה ולא כנתיב (`MsiExec.exe /X{GUID}` דרך `p.dirname` החזיר
  נתיב **יחסי**); מוחזרת רק תיקייה קיימת; `ERROR_MORE_DATA` בשם מפתח חריג
  מדלג עליו במקום לקטוע בשקט את כל שאר הרישומים; `REG_EXPAND_SZ` מורחב;
  והחוצץ עבר ל-`malloc`, כי אתחול-האפס של `calloc` בווינדוס הוא לולאת
  בייטים ב-Dart שרצה על 8KB לכל רישום הסרה במחשב.

## Unreleased — שורת כותרת משלנו, ותיקוני עמידות מסריקה מקיפה

שורת הכותרת של המערכת הוסתרה (`window_manager`, `TitleBarStyle.hidden`)
ובמקומה `AppTitleBar` — פורט של `custom_title_bar.dart` מאוצריא: הסמל והשם
בהתחלה, שם המסך באמצע, כפתורי החלון בסוף, וכל השאר גורר את החלון.

- **`SetupErrorScreen` קיבל גם הוא את השורה.** בלעדיה, המסך שמוצג כשתיקיית
  הנתונים אינה ניתנת לכתיבה היה חלון בלי סגירה, בלי מזעור ובלי גרירה —
  בדיוק במצב שבו אין למשתמש שום פעולה אחרת.
- **כשל באתחול `window_manager` אינו מונע את עליית התוכנה.** לכל היותר
  נשארת מסגרת המערכת.

תיקוני עמידות שעלו בסריקה של כל החבילות:

- **הורדה מלאה הכריזה על גרסת יעד שהנכס אינו מביא.** כש-ה-release האחרון
  מכיל patches בלבד, ה-DB המלא האחרון הוא של גרסה קודמת — והאימות שאחרי
  החילוץ דחה ~1.1GB שהורדו זה עתה. `LibraryDiscoveryResult` מחזיר עכשיו גם
  `latestFullDbVersion`, וזו גרסת היעד של מסלול ההורדה המלאה. הפער נסגר
  בבדיקה הבאה, שמוצאת מסלול דלתא משם ולמעלה.
- **החלפת המסד היא שני renames עם גלגול אחור**, במקום מחיקה ואז rename —
  ראו AGENTS.md §5. בדיקת "אוצריא פתוחה?" חוזרת רגע לפני ההחלפה,
  ו-`-wal`/`-shm` נמחקים רק אחרי שהיא הצליחה.
- **שרשרת דלתא שנקטעה באמצע רושמת את מה שכבר הוחל** ל-
  `.otzaria-external-update.json`, ו-`ExternalUpdateNotice` מצטבר במקום
  לדרוס — שני עדכונים לפני שאוצריא נפתחה לא מאבדים את הראשון.
- **כשל בכתיבת קובצי ה-state הזעירים אינו הופך עדכון שהצליח לכישלון.**
- **`releases.json` נכתב אטומית** (קובץ זמני + `flush` + rename). קטיעה
  באמצע הותירה JSON פגום שהמחשב הלא-מקוון קרא כ"מראה שבורה", למרות
  שכל הנכסים שלמים עליו.
- **סנכרון קבצים נלווים שנכשל אינו מרוקן את `companions.json`** — פריט
  שנכשל שומר את רשומתו הקודמת כל עוד הקובץ עדיין במראה. כשלים בהם מגיעים
  עכשיו ליומן, במקום להיעלם עד שמגלים אותם במחשב הלא-מקוון.
- **חנות התוספים: ארבעה מסלולים שהיו מוחקים נתונים.** רשימת תוספים ריקה
  מהאתר נדחית במקום לרוקן קטלוג קיים; דף בית בלי קטגוריות שומר את אלה
  שכבר במראה; הורדת קובץ בודד אינה מוחקת את הקטגוריות וטקסטי דף הבית;
  והורדה שנכשלה משאירה בקטלוג את גרסת הקובץ **שבמראה** — אחרת הקובץ החדש
  לא היה יורד לעולם וההתקנה הייתה מגישה את הישן תחת מספר הגרסה החדש.
  סנכרון עם אזהרות מדווח ככזה במקום כהצלחה מלאה.
- **`OtzariaAppMirror.sync` אינו מאבד ערוץ שכבר על הכונן** כשההורדה השנייה
  נכשלת, ואינו מוחק את כל קובצי ההתקנה כשה-API החזיר רשימה ריקה.
- **קריאת ההגדרות של אוצריא מסוריאלית ובשם קופסה ייחודי.** Hive מזהה
  קופסה פתוחה לפי שם בלבד ומתעלם מהנתיב, ולכן שתי קריאות מקבילות היו
  מקבלות את ההגדרות של שורש הנתונים הלא-נכון.
- **יחידות הגודל (ק״ב/מ״ב/ג״ב) והתאריך עברו ל-`otzaria_l10n`.** קודם
  "412 MB" הופיע בתוך משפט עברי, ותאריך עברי בגימטריה בתוך משפט אנגלי.

## Unreleased — גיבוי המסד בוטל

עותק שני של `seforim.db` (~1GB) על כונן נייד הכפיל את הדרישה מהכונן בלי
להוסיף ביטחון אמיתי — שני מסלולי ההחלה ממילא מותירים את המסד החי שלם עד
הרגע האחרון: מסלול patch עטוף ב-transaction יחיד שמתגלגל אחורה מעצמו,
ומסלול המסד המלא מחלץ ל-`<db>.new`, מאמת (`quick_check` + גרסה), ורק אז
מחליף ב-rename.

- **ההגדרה "גיבוי בטיחות של המסד" הוסרה**, ואיתה כרטיס "אחסון" כולו (הוא
  לא החזיק שום דבר אחר) והשדה `AppSettings.backupsToKeep`. `schemaVersion`
  עלה ל-4; מקטע `storage` בקובץ הגדרות קיים נבלע כמו כל מפתח לא מוכר.
- **`LibraryDbRecoveryService` כותב סימון (`<db>.applying`) ולא יותר.** אין
  יצירת גיבוי, אין שחזור ממנו, ואין `rollback` — כשל apply רק מנקה את
  הסימון. `RecoveryAction.restored` ו-`BackupIntegrityException` הוסרו,
  ו-`blockedMissingBackup` נקרא עכשיו `interrupted` (זה המצב היחיד שנשאר).
  שאריות `.backup` מגרסאות קודמות נמחקות בעלייה, כדי לא להשאיר ~1GB תלוי.
- **מסלול המסד המלא: אם ה-rename הוא שנכשל** — כלומר ה-DB החי כבר נמחק —
  `<db>.new` נשאר על הדיסק במקום להימחק גם הוא. זה הקובץ התקין היחיד
  שנשאר, וקודם היה הגיבוי שכיסה על המקרה הזה.

## Unreleased — בדיקת התהליך פעם אחת, ובמקביל

מדידה של מסלול העלייה (Release, מול תיקיית נתונים אמיתית) הראתה
ש-`checkAll` לוקח 764–1,529ms, ורובו המתנה מיותרת:

- **`tasklist` רץ פעמיים בכל עלייה.** פעם ב-`OtzariaManager.checkForUpdate`
  (זיהוי ההתקנה לפי התהליך הרץ) ופעם ב-`AppShell._refreshProcessState`
  (התצוגה "אוצריא פתוחה"), בטור, ~300ms כל אחת.
  `RunningOtzariaLocator.probe()` מחזיר עכשיו גם "רצה?" וגם את הנתיב
  מבדיקה אחת, התוצאה עוברת ב-`OtzariaUpdateCheckResult.isOtzariaRunning`,
  והלאנצ'ר מפסיק לשאול בעצמו. כשל בבדיקה עדיין מדווח "רצה", כמו
  `OtzariaProcessGuard` — אין החלשה של החסימה לפני כתיבה למסד.
- **שלושת שלבי `checkForUpdate` רצו בטור** אף שאינם תלויים זה בזה —
  קריאת המראה, קריאת ה-state ובדיקת התהליך. עכשיו במקביל.
- **איתור ה-`seforim.db` רץ פעמיים לכל בדיקת ספרייה** — פעם מ-
  `currentDbPath()` ופעם בתוך `checkForUpdate()` — וכל ריצה מעתיקה את
  קופסת ההגדרות של אוצריא ופותחת אותה ב-Hive. הקונטרולר קורא עכשיו את
  `LibraryManager.lastResolvedDbPath` מהבדיקה שכבר רצה.

התוצאה: 408–659ms במקום 764–1,529ms. הפריים הראשון (30–45ms) לא הושפע —
הוא ממילא לא היה חסום בכלום.

## Unreleased — יישור מלא לעדכון הספרייה המקוון של אוצריא

הצד השני של אותו מנגנון הוא `Otzaria/otzaria`, ב-`lib/library_update/`,
מעל אותה חבילת מנוע שיושבת בשורש המאגר הזה
(`Otzaria/otzaria_library_updater`). המנוע היה זהה כבר קודם — discovery,
planner, hasher, `PatchApplier`, `PatchDownloader`. מה שהיה שונה זו שכבת
התזמור, וזה מה שיושר כאן:

- **נתיב המסד נלקח מההגדרות של אוצריא עצמה.** הפער החמור מכולם: אוצריא
  קוראת `key-library-path` + `key-library-folder-name` מקופסת ה-Hive
  `app_preferences`, ואנחנו רק ניחשנו תיקיות ברירת מחדל. מי שהעביר את
  הספרייה לכונן אחר — הגדרה נתמכת ונפוצה — קיבל עדכון לקובץ אחר, או
  "התקנה טרייה" שאוצריא לעולם לא תראה. `OtzariaSettingsReader` קורא את
  הקופסה מעותק זמני, כדי לא ליצור נעילה בתיקייה של אוצריא. נוספו גם זיהוי
  התקנה ניידת (`portable.marker` → `otzaria_data`) וספרייה מצורפת של
  חבילת FULL.
- **הקבצים הנלווים נוסעים במראה.** אוצריא מרעננת בכל עדכון ספרייה גם את
  התלמוד הבבלי, את קטלוג otzar-HB ואת מילון החיפוש — שלושתם **מהרשת**,
  כלומר בדיוק מה שאין במחשב היעד. `CompanionAssetsMirror` מוריד אותם
  לצד המסד ל-`mirror/companions/`, ו-`CompanionAssetsInstaller` מתקין
  אותם אחרי המסד לאותם יעדים ועם אותם סימוני-גרסה שאוצריא בודקת.
- **דגלי ה-apply זהים לאלה של אוצריא** (`verifyFromHash: false`,
  `checkForeignKeys: false`) — חסך קריאה מלאה נוספת של מסד ~5.5GB לכל
  patch — ותת-השלבים והתקדמות ה-hash מדווחים ל-UI במקום מד שנראה תקוע.
- **`PRAGMA quick_check` לפני ההחלפה** במסלול ההורדה המלאה, ב-isolate.
  קודם החלפנו את המסד ורק אז בדקנו גרסה.
- **המראה שומרת חמישה releases** ולא אחד, כך שמכונה כמה גרסאות מאחור
  מקבלת שרשרת patches במקום הורדה של ~1.1GB.
- **אינדקס החיפוש** — הפער היחיד שנשאר פתוח, כי אי אפשר לסגור אותו מהצד
  שלנו: עדכון חיצוני עוקף את המסלול שבו אוצריא מאנדקסת מחדש את הספרים
  שהשתנו. `applyUpdate` כותב `.otzaria-external-update.json` עם המסלול,
  הגרסה ומזהי הספרים; הבקשה המדויקת לצד אוצריא מנוסחת ב-
  `library_manager/README.md`.

## Unreleased — שפה ומראה הם ההגדרות הראשונות

כרטיס "ממשק ותמיכה", שישב בתחתית מסך ההגדרות, פוצל לשניים:

- **"שפה ומראה"** — שפת הממשק, ערכת הנושא וגודל הטקסט — עלה לראש המסך,
  לפני האוטומציה. זו ההגדרה הראשונה שמישהו שפותח את התוכנה צריך, ולא
  הגיוני שכדי להחליף לעברית/אנגלית צריך לגלול מתחת לשלושה כרטיסים של
  אוטומציה, הורדה ואחסון.
- **"תמיכה"** — יומן הפעילות ואיפוס ההגדרות — נשאר בתחתית. איפוס הוא
  פעולה הרסנית־למחצה ואין לו מה לעשות בראש המסך.

ב-`otzaria_l10n` הוחלף `uiCardTitle` בשני מפתחות: `appearanceCardTitle`
(עם `appearanceCardSubtitle`) ו-`supportCardTitle`.

## Unreleased — זיהוי מיקום ההתקנה לפי התהליך הרץ

עד עכשיו הלאנצ'ר ידע להגיד "אוצריא פתוחה" אבל לא ידע איפה היא מותקנת:
אלה היו שני מנגנונים נפרדים לגמרי. הבדיקה אם היא רצה
(`OtzariaProcessGuard` ב-`library_manager`) מריצה `tasklist` עם שם
התהליך בלבד — ובפלט של `tasklist` אין נתיב כלל — ואילו איתור ההתקנה
(`OtzariaManager._autoDetectDirs`) רק ניחש רשימת תיקיות מוכרות
(`%LocalAppData%\Programs\Otzaria`, `%ProgramFiles%\Otzaria`,
`C:\אוצריא`). מי שהתקין במקום אחר נשאר בלי זיהוי, גם כשהתוכנה פתוחה מול
העיניים.

`RunningOtzariaLocator` החדש שולף את הנתיב מהתהליך עצמו — הראיה החזקה
ביותר שיש, כי זה בדיוק העותק שהמשתמש מפעיל:

- **Windows** — ה-PID מ-`tasklist` בפורמט CSV, והנתיב דרך Win32
  (`OpenProcess` עם `PROCESS_QUERY_LIMITED_INFORMATION` +
  `QueryFullProcessImageNameW`, `package:win32`, שכבר משמש לקריאת
  הגרסה). `wmic` לא נשקל: הוא הוסר מווינדוס 11 העדכני.
- **macOS** — `ps -A -o comm=` מדפיס נתיב מלא, ומשם עולים לשורש ה-`.app`.

`OtzariaManager.detectRunningInstall()` בונה מזה מצב התקנה מלא (גרסה
נקראת מהקובץ עצמו כרגיל), ו-`checkForUpdate()` מנסה אותו **לפני** רשימת
התיקיות. אין כאן סריקת תיקייה: ידוע בדיוק איזה קובץ רץ.

בשונה מהזיהוי לפי תיקייה, מה שזוהה מהתהליך גם **נשמר** ל-state — זו
תצפית חולפת, ובלי שמירה היא הייתה נעלמת בדיוק ברגע שבו מבקשים מהמשתמש
לסגור את אוצריא.

כשלון מחזיר null ונופל חזרה להתנהגות הקודמת — אוצריא סגורה, או שהיא רצה
כמנהל והלאנצ'ר לא (ואז `OpenProcess` נחסם). ההתאמה נשארה על שם התהליך
המדויק, ויש בדיקה ב-`launcher_app` שמאמתת ששתי החבילות מחזיקות את אותה
רשימת שמות — דריפט ביניהן היה מחזיר בדיוק את הבאג הזה.

**מה זה לא פותר:** מיקום ה-`seforim.db`. הוא יושב תחת `%APPDATA%` ללא
קשר לתיקיית ההתקנה, ולגזור אותו מהתהליך היה מחייב סריקת ה-handles
הפתוחים שלו.

## Unreleased — כרטיס "רשת" בהגדרות הוסר

ההגדרה היחידה שהייתה שם — "timeout להורדה", בחירה בין 10/20/45 שניות —
הוסרה על כל השרשרת: `AppSettings.networkTimeoutSeconds`, סעיף `network`
בקובץ ה-JSON, הזלגתה ב-`AppShell._applySettings`, ה-setters המקבילים
בשלושת קונטרולרי הלאנצ'ר, וארבע המחרוזות ב-`otzaria_l10n`.

זו הייתה שאלה שאין למשתמש שום דרך לענות עליה נכון: מי שההורדה שלו נכשלת
לא יודע אם הסיבה היא timeout קצר מדי, ומספר גדול יותר רק מאריך את
ההמתנה לפני אותה הודעת שגיאה. הזמן הקצוב נקבע מעכשיו בלקוחות עצמם, שם
הוא כוונן לפי סוג הפנייה: 15 שניות לבדיקת מטא-דאטה מול GitHub
(`GithubLibraryReleaseClient`), 20 שניות לפתיחת חיבור ולשאר הבקשות, ו-30
שניות של שתיקה בזרם ההורדה (`stallTimeout`) — שהוא ה-timeout שבאמת תופס
הורדה תקועה, ומעולם לא היה חשוף בהגדרות.

`schemaVersion` עלה ל-3. קובץ הגדרות ישן ממשיך להיקרא כרגיל; סעיף
`network` שבתוכו נבלע ככל מפתח לא מוכר.

## Unreleased — קובץ הרצה אחד בשורש הכונן

עד עכשיו ה-zip ל-Windows נפרס כערמה אחת: `launcher_app.exe` ביחד עם
עשרות קבצי DLL ותיקיית `data`, ומי שקיבל את הכונן היה צריך לזהות מי מכל
אלה הקובץ שלוחצים עליו. מעכשיו החילוץ מייצר בשורש שני פריטים בלבד:

```
עדכוני אוצריא.exe
app-files/
```

הכל נשאר portable — בלי מתקין, בלי רישום ב-Windows ובלי `Program Files`.
`launcher_app.exe` עצמו לא זז, כי הוא **חייב** את `flutter_windows.dll`
ואת `data/` צמודים אליו; מה שיושב בשורש הוא קובץ הרצה זעיר (`windows_stub/`)
שכל תפקידו להפעיל אותו, עם אותו אייקון ואותו שם. הנתונים הכבדים
(`OtzariaData/` — המסד, המתקינים והתוספים) ממשיכים להיווצר לצד הלאנצ'ר
האמיתי, כלומר בתוך `app-files/`, וכך השורש נשאר נקי.

זהו שינוי אריזה בלבד: אין שינוי בקוד ה-Dart.

## Unreleased — הגלילה בחנות התוספים

רשת הכרטיסים של החנות נבנתה בתוך `SliverLayoutBuilder`. ה-`scrollOffset`
הוא חלק מ-`SliverConstraints`, ולכן ה-builder רץ מחדש בכל פריים של גלילה,
ובכל פעם נוצר `SliverChildBuilderDelegate` חדש — ש-`shouldRebuild` שלו
מחזיר תמיד `true`. התוצאה: כל הכרטיסים הגלויים, על תמונותיהם, נבנו מחדש
בכל פריים ובתוך שלב ה-layout, והגלילה הרגישה תקועה.

חישוב מספר העמודות וגובה האריח עבר ל-`SliverGridDelegate` ייעודי, שמקבל
את רוחב הרשת בזמן הפריסה עצמה. הרשת היא מעכשיו `SliverGrid.builder` רגיל
שאינו נבנה מחדש בגלילה, וה-delegate מבצע relayout רק כשהגדלת הטקסט של
המשתמש משתנה. הנוסחאות עצמן לא שונו והפריסה זהה.

## Unreleased — התוכנה מדברת גם אנגלית

הלאנצ'ר תומך מעכשיו בשתי שפות: עברית — ברירת המחדל, כמו קודם — ואנגלית.
המתג נמצא בהגדרות, בכרטיס "ממשק ותמיכה", והחלפה נכנסת לתוקף מיד בלי
להפעיל מחדש. הבחירה נשמרת בקובץ ההגדרות (`ui.language`), וקובץ הגדרות
ישן שאין בו את השדה נטען לעברית.

**התרגום כולל גם את הודעות השגיאה.** זו הייתה הסיבה להוסיף חבילה שישית,
`otzaria_l10n`: חלק ניכר מהמלל שהמשתמש רואה בפועל לא נוצר במסכים אלא
בחבילות התשתית — "אוצריא פתוחה כרגע, יש לסגור אותה לפני עדכון המסד",
"אין גרסת אוצריא בתיקייה המקומית", שמות שלבי ההורדה והסנכרון — והוא מוצג
כמו שהוא. `otzaria_manager` ו-`plugins_manager` אינן מורשות לתלות
ב-Flutter, ולכן החבילה היא Dart טהורה ובלי תלויות בכלל; כל חמש החבילות
האחרות תלויות בה.

הנוסח באנגלית הוא תרגום חופשי ולא מילולי — אותה כוונה ואותו משלב, לא אותו
סדר מילים.

**מה לא מתורגם, בכוונה:** כל מה שמגיע מ-otzaria.org — שמות התוספים,
התיאורים, התגיות, שמות הקטגוריות והטקסטים של דף הבית של החנות. גם התאריך
העברי בחנות ("ט"ו בשבט ה'תשפ"ו") נשאר כפי שהוא בשתי השפות; מה שמתורגם
סביבו הוא רק התווית ("עודכן ב־" ← "Updated").

בדרך תוקנו כמה דברים שהיו נכונים רק ל-RTL: שדה הטקסט אכף כיוון מימין
לשמאל גם על קלט באנגלית, ההודעה הצפה, מסגרת הציטוט וההזחה של "מה התחדש",
הקו המפריד של סרגל הקטגוריות, וחצי הניווט בגלריית צילומי המסך.

## Unreleased — התוכנה נתקעת בהורדה, ו"יש עדכונים ברשת" שלא נכבה

שני ליקויים בזרימת ההורדה.

**התוכנה נחנקת באמצע ההורדה.** `IOSink.add` מכניס לתור ומחזיר מיד — הוא
אינו מפעיל לחץ-נגד. כשהיעד איטי מהרשת, וזה בדיוק המקרה כאן (כונן USB מול
חיבור ביתי), כל ההפרש נערם ב-RAM: בהורדת המסד (~1GB) אפשר להגיע למאות
מגה-בייטים תלויים בזיכרון עד שהתוכנה נעצרת. שלושת מסלולי הכתיבה
(`PatchDownloader._consumeBody`, `PatchDownloader._copyLocalFile`,
`OtzariaInstaller._download`) ממתינים מעכשיו ל-`flush` כל 4MB. ההמתנה גם
עוצרת את הזרם — הזיכרון חסום בגודל קבוע — וגם משחררת את לולאת האירועים,
כך שהחלון ממשיך להגיב ולצייר.

בנוסף, נכס שכבר יושב שלם על הדיסק מדלג על ההורדה ורק מאומת. אימות sha256
של 1.1GB מכונן נייד נמשך דקה ויותר, ועד עכשיו הוא רץ בלי דיווח כלשהו:
המד קפא על 100% ושם השלב נשאר "מוריד". `downloadToFile` מקבל
`onVerifyProgress`, ו-`LibraryMirrorExporter` מתרגם אותו לשלב "מאמת"
עם התקדמות אמיתית.

**"נמצאו עדכונים חדשים ברשת" נשאר דולק מיד אחרי הורדה מוצלחת.** הכרטיס
הזה שואל "יש ברשת משהו שעוד לא הורדנו?", אבל ההשוואה נעשתה מול הגרסה
ה**מותקנת**:

- `OtzariaModuleController.hasOnlineUpdate` השווה את התג שברשת מול
  `currentVersion` (המותקן) לפני `latestVersion` (מה שבמראה). מי שהוריד
  גרסה חדשה ועוד לא התקין אותה — וזה כל הרעיון של המחשב המקוון — ראה את
  ההודעה נשארת. ההשוואה היא מעכשיו מול המראה בלבד; ההתקנה היא צעד נפרד,
  והכרטיס של תוכנת אוצריא הוא זה שמדווח עליה.
- `LibraryManager.peekLatestOnlineVersion` ספר **כל** release כשיר, גם
  כזה שאין בו manifest ולא מסד מלא. הורדה מביאה רק releases עם תוכן מסד
  (`LibraryMirrorExporter._latestOnly`), וכך גם `discover` סופר — כלומר
  release אחד כזה ברשימה היה מציג "יש עדכון ברשת" שאף הורדה לא מסלקת.
  הבדיקה הקלה מסננת מעכשיו לפי אותו כלל בדיוק.

## Unreleased — אחוזים וגדלים במד ההורדה

ההורדה של המסד המלא נמשכת עשרות דקות, ועד עכשיו מד ההתקדמות שלה כמעט
לא זז: הוא נשען על ספירת **נכסים** שהושלמו, והמסד כולו הוא נכס אחד —
כך שהמשתמש ראה פס עומד ולא ידע אם משהו בכלל קורה.

**`launcher_app`:**

- `InfoProgressRow` מציג מעכשיו את האחוז לצד שם השלב, ושורת פירוט
  אופציונלית מתחתיו ("412 MB מתוך 1.1 GB"). האחוז מוצג רק כשיש יעד
  ידוע; בלעדיו המד נשאר לא-קבוע כמו קודם.
- `LibraryModuleController` מחווט את `onBytesProgress` של ההורדה
  וחושף `downloadProgress` — הנכסים שהושלמו ועוד החלק היחסי של הנכס
  שיורד כרגע. הבייטים מתאפסים בכל מעבר לנכס הבא, אחרת המד היה קופץ
  אחורה. השדות `applyReceivedBytes`/`applyTotalBytes` עושים את אותו
  דבר עבור החלת העדכון על המסד.
- שכבת הסנכרון של חנות התוספים מציגה גם היא אחוז.
- `services/byte_size.dart`: `formatBytes` / `formatBytesProgress`
  לתצוגת גדלים למשתמש.

**`seforim_library_updater`:**

- `LibraryMirrorExporter.export` מדווח `onAssetProgress(0, total)` לפני
  הנכס הראשון. בלי זה מספר הנכסים היה ידוע רק אחרי שהראשון (המסד,
  ~1GB) הסתיים — כלומר בדיוק כשכבר לא היה בו צורך.

## Unreleased — שתי גרסאות בכונן: יציבה ולא-יציבה, והבחירה ביניהן

עד עכשיו ההורדה הביאה גרסה אחת — הפרסום החדש ביותר ב-GitHub, בלי קשר
לכך שהוא מסומן `release` (יציב) או `pre-release` (לא יציב). מעכשיו היא
מביאה את **שתיהן**, ובמחשב המנותק המשתמש בוחר איזו מהן להתקין — בלי
לחזור לרשת.

**`otzaria_manager`:**

- `OtzariaReleaseClient.fetchChannelReleases()` מחליף את
  `fetchLatestRelease(allowPrerelease:)`: הוא מחזיר את ה-release היציב
  האחרון וגם את ה-pre-release האחרון, **רק אם הוא חדש מהיציב** (GitHub
  מחזיר את הרשימה מהחדש לישן, ולכן "חדש יותר" = מופיע לפניו). pre-release
  ותיק מהיציב אינו בחירה אמיתית ואינו יורד — וזה המצב באתר של אוצריא
  כרגע, שבו הפרסום החדש ביותר הוא release רגיל.
- `release` שאין לו קובץ התקנה לפלטפורמה הנוכחית **מדולג** במקום להפיל
  את כל הבדיקה; הערוץ ממשיך לגרסה הוותיקה יותר. `NoStableReleaseException`
  ירד: הוא נבע מכך שערוץ היה בחירה בזמן הורדה, וזה כבר לא המצב. כשאין
  שום release יציב ב-50 האחרונים, ה-pre-release הוא פשוט הגרסה היחידה
  שמוצעת — עם התווית שאומרת בדיוק את זה, לא כנפילה שקטה.
- `OtzariaAppMirror` מחזיקה מעכשיו **עד שתי רשומות**, לפי ערוץ, בקובץ
  `latest-release.json` (`schemaVersion: 2`). כל ערוץ נטען ונבדק בנפרד,
  כך שרשומה פגומה באחד לא פוסלת את השני, ומראה בפורמט הישן (רשומה בודדת
  בשורש) עדיין נקראת ומשויכת לערוץ לפי הדגל שלה — כונן קיים לא מתבטל.
  המטא־דאטה נכתבת מחדש אחרי כל הורדה שהסתיימה, והיציבה יורדת ראשונה, כדי
  שכישלון באמצע לא ישאיר את הכונן בלי גרסה להתקין.
- `OtzariaInstaller.pruneCacheExcept({keepTagNames})` במקום ניקוי
  ה-cache שהשאיר תג אחד בלבד — אחרת התקנה של ערוץ אחד הייתה מוחקת את
  קובץ ההתקנה של השני, ומחייבת הורדה מחדש כדי לחזור אליו.
- `OtzariaManager.allowPrerelease` (מה להוריד) הפך ל-`preferPrerelease`
  (מה להתקין), ו-`OtzariaUpdateCheckResult` חושף `stableRelease`,
  `prereleaseRelease`, `hasChannelChoice` ו-`selectedChannel` לצד
  `latestRelease` המחושב.

**`launcher_app`:**

- מסך "תוכנה" מציג פקד בחירה **רק כששתי הגרסאות בתיקייה** — עם מספרי
  הגרסאות עצמם ועם האזהרה שהלא-יציבה עלולה להכיל תקלות. אין בחירה כשאין
  מה לבחור. הבחירה נשמרת ב-`AppSettings.preferAppPrerelease`, וההחלפה לא
  נוגעת ברשת: שני קובצי ההתקנה כבר בכונן.
- דיאלוג ההתקנה (בדף הבית ובמסך התוכנה) אומר במפורש כשמתקינים את הגרסה
  הלא-יציבה, ומד ההתקדמות בהורדה נוקב באיזו משתי הגרסאות הוא עוסק —
  בלי זה הוא היה מתאפס באמצע בלי הסבר.

## Unreleased — "עדכוני אוצריא": שם, סמל, וסרגל עליון ממותג

**`launcher_app`:**

- **השם שמוצג למשתמש הוא מעכשיו "עדכוני אוצריא"** — כותרת החלון ב-Windows,
  `ProductName`/`FileDescription` במשאבי ה-exe, `CFBundleDisplayName`
  ב-macOS, וכותרת ה-`MaterialApp`. שמות הבנייה (`launcher_app.exe`,
  `PRODUCT_NAME = Otzaria Launcher`) נשארו באנגלית בכוונה: הם גם שם
  התהליך, ו-`OtzariaProcessGuard` מחפש שם את "אוצריא".
- **לתוכנה יש אייקון משל עצמה** — ספר פתוח עם חצי רענון, חלק פאזל וקובייה:
  `windows/runner/resources/app_icon.ico` (16–256, 32bpp) ואייקוני
  `AppIcon.appiconset` ב-macOS, במקום סמל ברירת המחדל של Flutter. המקור
  לשניהם הוא `assets/images/app_icon.png`.
- **הסרגל העליון ממשיך להציג את סמל אוצריא עצמו**
  (`assets/images/otzaria_logo.png`) — שני קבצים שונים בכוונה: האייקון מזהה
  את המעדכן בין הקבצים שבכונן, והסמל שבסרגל אומר לאיזו תוכנה הוא שייך.
- הסרגל העליון מציג מעכשיו את הסמל ואת השם בלבד. גלולות המצב (רשת,
  תיקיית הנתונים, "נבדק ב־") וכפתורי הבדיקה והיומן ירדו ממנו: אותו מידע
  כבר מופיע בדף הבית ובמסכי "תוכנה"/"ספרייה", ליד הפעולה שהוא מתאר,
  והיומן נפתח מההגדרות.
- **תוויות פקדי הסגמנטד (ערכת נושא, גודל טקסט, timeout, גיבוי) ישבו מתחת
  למרכז התיבה.** שני גורמים: סימן ה-וי על האפשרות הנבחרת מוסיף לסגמנט
  ריפוד אנכי משלו, ו-`SegmentedButton` מותח לפיו את *כל* הכפתורים; ומעבר
  לזה הוא מעתיק לסגמנטים רק חלק מהסגנון ו**משמיט ממנו את
  `minimumSize`/`maximumSize`**, כך שהגובה שנקבע להם לא נכנס לתוקף כלל
  והם נשארו על 40 בתוך תיבה של 32. הסימן הוסר (הרקע המלא כבר מסמן את
  הנבחר), הגובה נקבע דרך `visualDensity` — הדרך היחידה שכן עוברת — וכל
  התיבות אחידות על 32.

## Unreleased — חנות התוספים לפי העיצוב מחדש שבאתר

האתר (`Otzaria/Otzaria_Website`) בנה מחדש את החנות: קטגוריות מנוהלות
במקום תגיות כניווט ראשי, "תוספים נבחרים" אצורים, טקסטים של דף בית
שנערכים בפאנל הניהול, ושלושה דפים במקום אחד. המראה האופליינית נושאת
עכשיו את המבנה הזה איתה, והמסך בלאנצ'ר פורס אותו כמו שם.

**`plugins_manager`:**

- הסנכרון מושך שתי קריאות נוספות — `/api/plugins/store-home` (טקסטים
  ורשימת הקטגוריות הגלויות) ו-`/api/plugins/categories/<slug>` לכל
  קטגוריה (החברות המלאה בסדר הידני של האתר; דף הבית מחזיר תוספים רק
  לקטגוריות שמסומנות להצגה בו, ולכן הוא לבדו אינו מספיק).
- `catalog.json` גדל ב-`categories` (slug, שם, תיאור, שורת דף-בית ומזהי
  התוספים בסדר) וב-`home` (כותרת ותקציר), ועל כל תוסף נשמר `categories` —
  רשימת ה-slugs שהוא משובץ בהם, כדי שסינון אופליין יהיה בדיקת שדה ולא
  חיפוש הפוך.
- **כשל במבנה אינו מפיל סנכרון**: אתר ישן בלי הנתיבים האלה, או קריאה
  שנפלה, מדווחים כ-`warning` והמראה שומרת את הקטגוריות הקודמות. תוסף
  שאינו בקטלוג (הוסתר / נמחק) מדולג בשקט ואינו נספר בקטגוריה.
- `isPinned` באתר פירושו כיום "נבחר" (featured) והשם נשאר שם לתאימות;
  כאן הוא `StorePlugin.isFeatured`, וקטלוג ישן עם `isPinned` עדיין נקרא.
- `matchesQuery` מחפש גם בשם המפתח, כמו החיפוש החכם של האתר (בלי דירוג —
  `/api/plugins/search` הוא צד-שרת ולכן חסר תועלת במחשב לא-מקוון).

**`launcher_app` — מסך החנות נפרס כמו באתר**, שלושה מסכים במקום רשימה
אחת: דף בית אצור (hero עם הכותרת והתקציר מהאתר וחיפוש, סעיף "תוספים
נבחרים" עם "הצג עוד נבחרים", שורת תוספים לכל קטגוריית דף-בית עם "לכל
הקטגוריה", ופס גילוי), "כל התוספים" (פירורי לחם, כרטיס הסינון והרשת),
ודף קטגוריה (שם, תיאור, מונה, והרשת בסדר הידני מהאתר). הניווט ביניהם
הוא סרגל צד של קטגוריות — מעל 1080px סרגל קבוע, מתחת לזה שורת צ'יפים —
עם "כל התוספים" מוצנע בתחתיתו. **הסינון קיים רק ב"כל התוספים"**, כמו
`/plugins/all` באתר. תוסף נבחר מסומן בגלולת "נבחר" (קודם: "מומלץ"),
ובעמוד התוסף נוספו גלולות קטגוריה שפותחות את דף הקטגוריה. מראה בלי
קטגוריות נפתחת ישר ב"כל התוספים".

- **גלילה:** `PluginStoreBody` מקבל עכשיו slivers בלבד, וסרגל הצד יושב
  מחוץ ל-`CustomScrollView`. קודם כל תוכן המסך ישב ב-`SliverList` עם
  רשימת ילדים קבועה — כלומר נבנה במלואו מראש — וכרטיס הסינון שגדל דחף
  את הרשת מטה. אין באזור הגלילה שום גליל מקונן: גליל אופקי כזה היה בולע
  את גלגלת העכבר ומקפיא את גלילת העמוד.
- **גודל הכרטיס:** הרוחב המינימלי ירד מ-300 ל-280, כך שהעמודה השלישית
  נפתחת בערך באותו רוחב שבו האתר עובר ל-`xl:grid-cols-3` — קודם חלון
  בינוני קיבל שתי עמודות עם כרטיסים רחבים בהרבה מאלה שבאתר.
- **גלישת הכרטיס תוקנה.** הכרטיס ברשת הוא בגובה קבוע (`mainAxisExtent`),
  ושתי שורות הגלולות שבו הן `Wrap` — כלומר גובה משתנה: שבב
  "עדכון זמין (מותקן …)" או ארבע תגיות ארוכות בכרטיס צר גלשו לשורה נוספת
  והכרטיס גלש. עכשיו לשורות יש תקציב גובה קבוע עם חיתוך, השבב בכרטיס
  מקוצר ל"עדכון זמין" (הפירוט נשאר בעמוד התוסף), והגובה הכולל הותאם.
  נוספה בדיקה עם התוכן הכבד ביותר בכרטיס הצר ביותר, גם בהגדלת טקסט.
- **שורת הסנכרון נשברת לשתי שורות** בחלון צר במקום לגלוש.

**מתג "רק מה שלא מותקן" חזר, ועבר לקצה השמאלי של שורת הסנכרון** — גלולה
קטנה שזמינה מכל מסכי החנות, ולא רק מ"כל התוספים". בשונה משאר הסינון הוא
חל גם על האצירה (נבחרים, שורות דף-הבית ודף קטגוריה), כי זו בדיוק המטרה
של הלאנצ'ר: לראות מה חסר. כשהמתג מסתיר את כל מה שהיה בדף מוצג הסבר
("הכול מותקן ומעודכן") עם כפתור לכיבויו, ולא מצב ריק מטעה.

**בדיקות:** סנכרון מלא מול אתר מדומה (`MockClient`) — תוספים, קטגוריות,
טקסטים, דילוג על מזהה רפאים, ושמירת המבנה הקודם כשהאתר מחזיר 404 — ובדיקת
ממשק שעוברת דף בית ← דף קטגוריה ← כל התוספים.

## Unreleased — עלייה מהירה יותר, זיכרון נמוך יותר, ותיקוני חוסן

**זיכרון: המסד המלא כבר לא עובר דרך ה-RAM.**

- `ZstdFileDecompressor` (חדש, ב-`library_manager`) מחלץ `.zst` **בזרימה**
  קובץ-לקובץ דרך `ZSTD_decompressStream`. במסלול `fullDownload` היה עד כה
  חילוץ בזיכרון: הקובץ הדחוס במלואו, ה-DB המחולץ (~1.1GB) כ-`Uint8List`, ועוד
  העתק שלו כשהוא נשלח ל-`Isolate.run` שכתב אותו — כלומר שיא של יותר מ-2GB על
  התקנה של מסד אחד. עכשיו שיא הזיכרון הוא חוצצים בודדים, ללא תלות בגודל המסד,
  והכתיבה היא `rename` באותו volume. כשאין ספריית zstd לטעינה נופלים למסלול
  הזיכרון הקודם, כך שזו אופטימיזציה ולא דרישה.
- חנות התוספים: התמונות מפוענחות בגודל התצוגה (`cacheWidth`) ולא בגודל המקור —
  תמונת קטלוג טיפוסית ירדה מ-~3.8MB לכמה מאות KB, כפול מספר התוספים. בנוסף
  הרשת היא `SliverGrid` אמיתי; קודם היא הייתה `GridView(shrinkWrap: true)` בתוך
  `ListView`, ו-`shrinkWrap` מבטל וירטואליזציה — כל הכרטיסים והתמונות שלהם נבנו
  יחד, גם מה שמחוץ למסך.
- `AppLogger` תוחם את עצמו: תור כתיבה יחיד (במקום `writeAsString` נפרד ובלתי
  מסודר לכל שורה, שיכול היה לערבב שורות), חסם על שורות ממתינות, ורוטציה ב-2MB
  כדי שהיומן לא יגדל בלי גבול על הכונן הנייד.

**עלייה: פחות עבודה לפני הפריים הראשון.**

- `AppShell` בונה מסך רק בכניסה הראשונה אליו. `IndexedStack` בונה את כל ילדיו,
  ולכן חנות התוספים כולה — כולל פענוח כל התמונות — נבנתה בעלייה, לפני שהמשתמש
  פתח את הלשונית. **תוצאה נלווית:** הודעת "יש עדכונים לתוספים" מוצגת בכניסה
  הראשונה ללשונית התוספים, ולא בפתיחת התוכנה.
- אתחול הלוגר וטעינת ההגדרות רצים במקביל (`Future.wait`) ולא בטור — סבב I/O
  אחד פחות לפני `runApp`, מורגש על כונן USB.
- `tasklist` רץ פעם אחת בעלייה ולא פעמיים (`checkAll` כבר מרענן את מצב התהליך).
- דיווחי התקדמות מדוללים דרך `ProgressNotifier` (~10 בשנייה). קודם כל צ׳אנק
  של הורדה הפעיל `notifyListeners` → `setState` על `AppShell` → בנייה מחדש של
  כל עץ ה-widgets; בהורדת מסד של 1GB אלה עשרות אלפי בניות. `filtered`/`allTags`/
  `updatablePlugins` בחנות ממוזנים ולא מחושבים מחדש בכל בנייה.

**חוסן רשת.**

- ההגדרה "זמן קצוב לרשת" הייתה **מוצגת ונשמרת אך לא בשימוש**: אף לקוח לא כיבד
  אותה, ול-`OtzariaReleaseClient`, `OtzariaChangelogClient`, `PluginStoreClient`
  ו-`OtzariaInstaller` לא היה timeout בכלל — מחשב שמחובר לרשת בלי מסלול
  לאינטרנט (captive portal) תלה את בדיקת העדכונים בפתיחה ללא הגבלה. עכשיו
  ההגדרה מוזלגת לכל המודולים ונכנסת לתוקף בבקשה הבאה.
- הורדת ה-installer מוחקת קובץ חלקי בכשל, כדי שהריצה הבאה לא תזהה אותו כ-cache
  תקין לפי גודל.
- `LibraryMirrorExporter` מוריד דרך `PatchDownloader` במקום `http` ישר, ולכן
  מקבל אימות גודל ו-sha256, חידוש הורדה (Range + If-Range) ומחיקת קובץ חלקי
  שאינו ניתן לחידוש. קודם הורדת המסד המלא (~1GB) על חיבור שנפל התחילה מאפס בכל
  פעם, ושרת שנשתק תלה את הייצוא בלי גבול.

**תיקוני בדיקות ואנליזה שנכשלו רק ב-Windows.**

- `patch_tables_contract_test` נכשל בכל מכונת Windows: ה-fixture נבדק בית-אחר-בית
  והוא נמשך מ-git כ-CRLF. נוסף `.gitattributes` שמקבע `*.json` ל-LF.
- `library_db_recovery_service_test` השאיר חיבור SQLite פתוח בבדיקת ה-hot journal,
  וב-Windows זה חסם את מחיקת תיקיית ה-temp ב-`tearDown`.
- `LibraryDbLocator.defaultDbDirs` בנה נתיבי macOS עם המפריד של המכונה המריצה,
  ולכן שתי בדיקות נכשלו ב-Windows. הפונקציה מקבלת `operatingSystem` כפרמטר, ולכן
  היא בוחרת עכשיו `p.posix`/`p.windows` לפיו.
- `library_manager` ו-`otzaria_manager` לא נבדקו על ידי האנליזה בכלל: בלי
  `analysis_options.yaml` משלהם הם ירשו את קובץ השורש — שמחריג אותם. שניהם קיבלו
  קובץ משלהם; שלוש הערות שהתגלו מיד תוקנו, וכן שלוש `annotate_overrides`
  ב-`GithubLibraryReleaseClient`.
- הוסרו שתי פונקציות top-level מתות ב-`LibraryUpdateApplier`
  (`_runApplyPatchInIsolate`, `_runWriteBytesInIsolate`). הן שרדו כי הפניה מתוך
  doc-comment נחשבת שימוש עבור `unused_element`.

## Unreleased — מצב אופליין יחיד, ותיקייה צמודה לתוכנה

עד כה התוכנה עבדה בכפילות: גם ניסתה לעדכן ישירות מהרשת, וגם בנתה מראה
אופליין. אין בכך צורך — אוצריא עצמה יודעת לעדכן דרך הרשת. מעכשיו יש **מצב
אחד**: הורדה אל תיקייה שצמודה לקובץ ההרצה, והתקנה משם.

**תיקיית הנתונים צמודה לתוכנה ואינה ניתנת לשינוי.**

- `AppPaths` (חדש) מאתר את `<תיקיית ה-exe>/OtzariaData` ובודק כתיבה בפועל.
  ב-macOS התיקייה נוצרת ליד חבילת ה-`.app`, לא בתוכה.
- כשאין הרשאת כתיבה התוכנה **מסרבת לרוץ** ומציגה `SetupErrorScreen` עם הסבר,
  במקום ליפול ל-`%APPDATA%` — נפילה כזו הייתה משאירה את הנתונים על המחשב
  המקוון, כלומר שוברת בשקט את כל הרעיון.
- כל ההגדרות של נתיבים הוסרו: `otzariaInstallPath`, `libraryPath`,
  `pluginsPath`, `preferredUsbPath`. `AppSettings.schemaVersion` = 2; קובץ מגרסה 1
  נקרא בלי לזרוק, והשדות שהוסרו פשוט מתעלמים.

**הרשת נוגעת בדבר אחד — ההורדה.**

- `LibraryManager`: `useLocalMirror()`/`useCloud()`/`currentLocalMirrorPath()`
  ו-`exportOfflineMirror(destDir:)` הוסרו. במקומם `downloadToMirror()`, ו-
  `_resolveSource()` מחזיר תמיד את המראה המקומית או זורק
  `LibraryMirrorMissingException`. **אין יותר נפילה לענן** — היא הייתה גורמת
  להתנהגות שונה תלוי אם המחשב מחובר.
- `OtzariaAppMirror` (חדש) — הפער האמיתי: מודול התוכנה לא היה לו מראה בכלל,
  ולכן במחשב אופליין הוא נכשל. עכשיו `latest-release.json` נשמר לצד קובץ
  ההתקנה, `checkForUpdate()` קורא ממנו בלי רשת, ו-`OtzariaInstaller.installFromFile`
  מתקין מהדיסק. `load()` פוסלת מראה חלקית (קובץ חסר או בגודל שגוי).
- "מה התחדש" בגרסה מוצג ממקור אחר: `OtzariaChangelogClient` (חדש) שולף את
  הפסקה המתאימה מיומן השינויים המרוכז של אוצריא (`assets/יומן שינויים.md`
  בענף `dev`), ומעדיף אותה על פני `release.body` הגולמי מ-GitHub, שלעיתים
  ריק. נשמר לצד שאר המטא־דאטה במראה המקומית, כך שגם קריא בלי רשת.
- כרטיס "מקור העדכון", כרטיס "תוכן להעברה" וכרטיס "נתיבים ואחסון" הוסרו
  מהממשק. במקומם כרטיס הורדה אחד עם בחירת רכיבים (`syncApp`/`syncLibrary`/
  `syncPlugins`), שרצים בטור.
- ההתקנה האוטומטית (`autoInstallApp`/`autoInstallLibrary`) הייתה מתג מת —
  הוצג בממשק ולא חובר לשום דבר. עכשיו היא מחווטת ב-`AppShell.checkAll`,
  ומדלגת על עדכון מסד כשאוצריא פתוחה.

**ערוצי גרסאות = דגל `prerelease` של GitHub.** release רגיל הוא יציב,
pre-release אינו. הערוץ מחווט בפועל ל-`LibraryManager` ול-`OtzariaManager`
(קודם הוא היה קיים בהגדרות ולא השפיע). מכיוון שהריפו של אוצריא כמעט לא
מפרסם יציב, ערוץ "יציב בלבד" זורק `NoStableReleaseException` שמסביר לעבור
ערוץ — במקום ליפול בשקט ל-pre-release.

**שני תיקוני נכונות שהתגלו בדרך:**

- **עדכון מסד ללא שינוי מספר גרסה היה בלתי־נראה.** SeforimLibrary מפרסם
  לפעמים מסד מתוקן באותו `db_version`, ו-`LibraryUpdatePlanner` החזיר `none`
  כי הוא השווה מספרים בלבד. עכשיו הוא משווה גם את ה-release tag שהוחל
  (`LibraryStateStore.appliedReleaseTag`) — אך ורק כשהוא ידוע, כדי לא להציע
  הורדה של ~1GB בכל פתיחה על סמך ניחוש ב-DB שלא הותקן דרך הלאנצ'ר.
- **המראה כללה את כל היסטוריית ה-patches** והגיעה לכמה ג'יגה-בייט — לא
  מתאים לכונן נייד. `LibraryMirrorExporter` מוריד עכשיו רק את ה-release
  האחרון (ואת האחרון שנושא DB מלא, אם שונה).

## חנות התוספים האופליינית

חלק התוספים בלאנצ'ר הופסק מלהיות "פריסה ומצב ריק" והפך למודול עובד. הבסיס
הוא המרה ישירה ל-Flutter של חנות התוספים האופליינית שנבנתה ב-Electron
(`Yehuda-Zakesh/Offline-repository-plugin-store`), שנגזרה בעצמה מחנות
התוספים באתר (`Otzaria/Otzaria_Website`).

**`plugins_manager` (חבילה חדשה)** — Dart טהור, במקביל ל-`otzaria_manager`:

- `PluginsManager` — ה-facade היחיד: `sync()` (במחשב המקוון) מול
  `load()`/`directInstall()`/`saveCopy()` (במחשב הלא-מקוון).
- `PluginStoreClient` — `GET otzaria.org/api/plugins` והורדת נכסים, כולל
  הסקת סיומת מ-`Content-Disposition` (גם `filename*=UTF-8''` לשמות עבריים)
  ומ-`Content-Type`.
- `PluginMirrorStore` — הקטלוג והקבצים נשמרים ב-`<mirrorDir>/plugins/`,
  כלומר **בתוך המראה הקיימת של הספרייה**: העתקה אחת ל-USB מעבירה את
  שתיהן. הנתיבים בקטלוג יחסיים ועם `/`, כדי שהמראה תעבוד גם מאות כונן
  אחרת וגם כשנכתבה בווינדוס ונקראת ב-macOS. הכתיבה אטומית.
- `PluginMirrorSync` — פורט של `syncNow`: דילוג על הורדה חוזרת של קובץ
  תוסף שגרסתו לא השתנתה, ותמונות/צילומי מסך שכן מתעדכנים בכל סנכרון.
  כשל בנכס בודד הופך לאזהרה ואינו מפיל את הסנכרון.
- `PluginManifestReader` — מחלץ את ה-`id` האמיתי מ-`manifest.json` שבתוך
  ה-`.otzplugin` (כולל הסרת BOM). ⚠️ זהו המפתח היחיד להשוואה מול
  התוספים המותקנים; ה-`id` שה-API מחזיר הוא מזהה מסד-הנתונים של האתר.
- `InstalledPluginsScanner` — סורק
  `<pluginsDir>/installed/<manifestId>/current/manifest.json`. הנתיב
  **מתגלה ולא מונח**, כמו ב-`LibraryDbLocator`.
- `PluginDirectInstaller` — התקנה דרך
  `otzaria://plugin/install-local?path=` בלבד, שעובדת בלי רשת. אין
  חילוץ ZIP עצמאי לתוך תיקיות אוצריא, כדי לא לעקוף את הרישום הפנימי
  של אוצריא. הכישלון מוחזר כערך ולא כחריג, כמו ב-`FileReveal`.
- 35 בדיקות יחידה עוברות.

**`launcher_app`**:

- `PluginsModuleController` — עוטף את `PluginsManager` כ-`ChangeNotifier`,
  עם חיפוש, סינון סטטוס/תגית, ומתג "רק לא-מותקן או שיש לו עדכון" (פועל
  כברירת מחדל, כמו במקור).
- `screens/plugins/` — חנות מלאה: רשת כרטיסים עם תמונות וגלולות, עמוד
  פרטי תוסף, גלריית צילומי מסך עם lightbox (חצים ומקלדת), שכבת סנכרון עם
  התקדמות ואזהרות, והודעת "יש עדכונים זמינים" בכניסה.
- `services/hebrew_date.dart` — המרה גרגוריאני→עברי + גימטריה. נדרש כי
  ל-`package:intl` אין לוח שנה עברי, בשונה מ-`Intl` בדפדפן שהחנות
  המקורית השתמשה בו.
- כרטיס התוספים בדף הבית מציג נתונים אמיתיים במקום "המודול בבנייה".

**הגדרות** — כל החלקים שנגעו בתוספים היו טקסט מציין-עתיד ("יתאפשר כשמודול
התוספים ייבנה") או שדות שלא חוברו לכלום. כולם עודכנו:

- `pluginsPath` חובר לבחירת תיקייה, ומוזן ל-`InstalledPluginsScanner`.
- **"סנכרון חנות התוספים בפתיחה"** (`autoDownloadAllPlugins`) — מחווט
  בפועל ב-`AppShell._loadPlugins`: כבוי בברירת מחדל, נחסם כש-`offlineOnly`
  פועל, ודורש אישור באזהרה כי זו הורדה ברשת בכל פתיחה. מסנכרן בלבד —
  התקנה נשארת יזומה תמיד.
- שני מתגי "תוספים מותקנים" נשארים מושבתים, אבל עם הסבר **אמיתי** במקום
  "המודול טרם נבנה": סנכרון חלקי אינו קיים (`PluginMirrorSync` מביא את כל
  הקטלוג), והתקנה פותחת את אוצריא לכל תוסף בנפרד ולכן אינה מתאימה לרקע.
- **`pluginsChannel`** חדל להתחזות לערוץ prerelease. לתוספים יש `status`
  לכל תוסף, ולכן הוא קובע כעת את סינון ברירת המחדל שהחנות נפתחת בו
  (`pluginStatusFilterFor`), עם תוויות "יציב בלבד" / "כולל בטא וניסיוני".
  שינוי בהגדרות נכנס לתוקף מיד, בלי לדרוס סינון שהמשתמש בחר ידנית בחנות.

**לא מומש בכוונה**: הורדת גרסה היסטורית של תוסף (ה-API מחזיר `versions`),
וסנכרון חלקי של המותקנים בלבד.

**לא אומת**: המסלול המלא מקצה לקצה — סנכרון אמיתי מ-`otzaria.org`, העברה
ב-USB, ופתיחת הפרוטוקול מול התקנה אמיתית של אוצריא.

## Unreleased — תמיכת macOS בלאנצ'ר

הלאנצ'ר (`launcher_app` + `otzaria_manager` + `library_manager`) עובד מעכשיו
גם ב-macOS 10.15+, לצד Windows. ה-package הראשי (`seforim_library_updater`)
לא נדרש לשינוי — הוא היה כבר cross-platform.

**`otzaria_manager`** — הופשט מהנחות Windows והפך לדו-מסלולי:

- נוסף `OtzariaTargetPlatform` (windows/macos), שניתן לדרוס בבדיקות; כל
  השירותים בוחרים מסלול לפיו במקום לפי `Platform` ישירות.
- `OtzariaRelease` — השדות `windowsInstaller*` שונו ל-`installer*` ונוסף
  `installerKind`; `NoWindowsAssetException` → `NoInstallerAssetException`.
  ⚠️ שינוי API שובר, בתוך הרפו בלבד.
- `OtzariaAssetSelector` (חדש) בוחר `otzaria-macos.zip` (או `.dmg` כגיבוי)
  ב-macOS ו-`*-windows.exe` בווינדוס. חבילות ה-FULL של ~2GB נפסלות בשתיהן.
- `OtzariaExeLocator` → `OtzariaAppLocator`: מאתר גם חבילות `.app` (הרדודה
  ביותר, בלי להיכנס לתוכה), עם סינון אופציונלי לתיקיות משותפות.
- `OtzariaInstaller` — מסלול התקנה ל-macOS: חילוץ עם `ditto` (שומר על
  החתימה, בשונה מ-`unzip`) לתיקיית staging, החלפה אטומית של ה-`.app`
  הקיים עם שחזור אם ההחלפה נכשלה, והסרת `com.apple.quarantine`.
- `MacAppVersionReader` (חדש) קורא `CFBundleShortVersionString` /
  `CFBundleIdentifier` מ-`Info.plist` דרך `plutil` (הקובץ הוא binary plist).
  הוא ו-`WindowsExeVersionReader` מממשים ממשק משותף (`InstalledVersionReader`).
- `OtzariaLauncher` — מפעיל `.app` דרך `open`. תוקן גם באג אמיתי: `.app`
  היא תיקייה, ולכן בדיקת `File.exists` עליה נכשלה על התקנה תקינה.
- `OtzariaUpdateCheckResult.updateAvailable` מנרמל גרסאות (מסיר `v` מוביל
  ואת מה שאחרי `+`) — בלי זה זיהוי התקנה קיימת (שמדווחת `0.9.96` מול תג
  `0.9.96+736`) נראה כמו "יש עדכון" ומוריד 73MB לחינם בכל פתיחה.
- `OtzariaInstallState.exePath` → `launchPath`, עם קריאה של המפתח הישן
  מה-JSON כדי שהתקנה קיימת של משתמש Windows לא "תישכח".
- ב-macOS גם `/Applications` נסרקת לזיהוי התקנה קיימת — עם אימות זהות
  (שם/`CFBundleIdentifier`), אחרת הסריקה הייתה מחזירה אפליקציה זרה.

**`library_manager`**:

- `OtzariaProcessGuard` עובד ב-macOS/לינוקס דרך `pgrep -x` (שם מלא בלבד,
  כדי לא לזהות את הלאנצ'ר עצמו). שם התהליך של אוצריא ב-macOS הוא `אוצריא`.
- הבדיקה "האם אוצריא פתוחה" אינה מדולגת יותר מחוץ לווינדוס.
- `LibraryDbLocator` מכיר את ברירות המחדל של macOS
  (`~/Library/Application Support/otzaria/books`, ובהתקנה מערכתית
  `/Library/...`) ואת `%ProgramData%\otzaria\books` בווינדוס — נגזר מ-
  `app_paths.dart` של אוצריא. הנתיבים ניתנים להזרקה, כך שהבדיקות לא
  תלויות בהתקנה אמיתית על מכונת המפתח.

**`launcher_app`**:

- נוספה תיקיית `macos/` (**נשמרת בגיט**, בשונה מ-`windows/`): bundle id
  `org.otzaria.launcher`, `CFBundleDisplayName` בעברית, App Sandbox כבוי
  (הכרחי — הלאנצ'ר כותב חבילות `.app`, נוגע ב-DB מחוץ ל-container ומריץ
  כלי מערכת), ו-`PRODUCT_NAME = Otzaria Launcher` שחייב להישאר שונה מ-
  `אוצריא` כדי שה-process guard לא יזהה את הלאנצ'ר כאוצריא פתוחה.
- `FileReveal` (חדש) מחליף את הקריאות הישירות ל-`explorer.exe` בפתיחה
  ב-Explorer/Finder לפי הפלטפורמה.

**CI**: `build-macos.yml` (חדש) + ג'וב macOS ל-`launcher_app` ומטריצת
`ubuntu`+`macos` ל-`otzaria_manager` ב-`ci.yml`. האריזה היא `ditto` ולא
`zip`, כדי לא לשבור את חתימת ה-bundle.

## 0.2.0

- הומרה לחבילת **Flutter** (`pubspec.yaml` מוסיף תלות ב-`flutter: sdk: flutter`
  ובאילוץ `environment.flutter`).
- נוסף `sqlite3_flutter_libs` לבינדינג הנייטיב של SQLite בכל פלטפורמות Flutter.
- `analysis_options.yaml` עבר מ-`lints` ל-`flutter_lints`; נוסף `flutter_test`
  ל-`dev_dependencies` (בנוסף ל-`test` הקיים, שממשיך לשמש את קבצי הבדיקה).
- אין שינוי בלוגיקת המודלים/השירותים תחת `lib/src` — Dart טהור, ללא תלות ב-widgets.

## 0.1.0

גרסה ראשונית — הוצאה מ-`otzaria/lib/library_update/` לחבילת Dart עצמאית.

- **מקור:** commit `d6d4e9facf5da322e83bdfbc199b899d3b210915` בריפו Otzaria.
- מנוע צריכת הפצות SeforimLibrary: מודלים, גילוי/תכנון מסלול, הורדה ואימות,
  hash לוגי (תואם `LogicalContentHasher.kt` של Kotlin), והחלת patch אטומית.
- חבילת Dart טהורה — ללא תלות ב-Flutter. חילוץ zstd מוזרק על-ידי הצרכן.
