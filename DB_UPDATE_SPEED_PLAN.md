# תוכנית עבודה: קיצור זמן עדכון המסד

מטרה: לקצר את הזמן שמשתמש מחכה בעדכון `seforim.db`, **בלי לוותר על אף
בדיקת בטיחות** שמגנה עליו מפני מסד שגוי או פגום.

נכתב ב-2026-08-11 על בסיס מדידות בפועל (למטה). כל שלב הוא commit נפרד
ובר-שילוח בנפרד.

---

## 1. איפה הזמן נשרף

עדכון דלתאי אחד = הורדת ה-patch, החלתו על המסד, ואז **אימות hash לוגי על
כל המסד** (`LogicalContentHasher.compute` — קריאת כל השורות בכל 34
הטבלאות, קידודן לזרם בתים, ו-SHA-256 עליו). האימות הזה הוא הערובה
היחידה לכך שהמסד שיצא נכון, והוא רץ **בכל צעד בשרשרת** — משתמש שפספס
חמש גרסאות משלם אותו חמש פעמים.

### מה נמדד — מיקרו-בנצ'מרקים

| מה נמדד | קצב |
| --- | --- |
| הצינור המלא של `compute` (מסד סינתטי 250MB) | **23 MB/s** |
| קריאת SQLite לבד (אותו SELECT, בלי hash) | 106 MB/s |
| SHA-256 של `package:crypto` — JIT / AOT | 42 / 50–58 MB/s |
| SHA-256 של Windows (`bcrypt.dll` דרך FFI) | **1,225 MB/s** |
| העתקה מ-`Uint8List` לזיכרון native (`setRange`) | 8–9 GB/s — **לא** צוואר בקבוק |

### מה נמדד — על המסד האמיתי (7,534MB קובץ, 5,896MB תוכן לוגי, `db_version=20`)

ארבעה סבבים **מתחלפים** על טבלת `line` (4,505MB לוגיים), כדי לנטרל את
מטמון הדיסק:

| סבב | מסלול | זמן | קצב |
| --- | --- | --- | --- |
| 1 (דיסק קר) | נייטיבי | 412.7s | 11 MB/s |
| 2 | `package:crypto` | 239.5s | 19 MB/s |
| 3 (מטמון חם) | **נייטיבי** | **68.1s** | **66 MB/s** |
| 4 (מטמון חם) | `package:crypto` | 209.4s | 22 MB/s |

ההשוואה ההוגנת היא בין שני הסבבים החמים: **פי 3**. ה-hash זהה בשני
המסלולים על 5,896MB אמיתיים. אזהרה למי שימדוד שוב: מעבר ראשון על מסד
קר-מטמון נראה איטי פי 6 ומטעה לגמרי — למדוד רק בסבבים מתחלפים.

### זמן סריקה לכל טבלה (ORDER BY של ה-hash, בלי חישוב hash)

סך הסריקות: **133.5s**. הפריטים הכבדים:

| טבלה | שורות | זמן | מיון חיצוני? |
| --- | --- | --- | --- |
| `line` | 5,888,636 | 73.6s | לא (סריקת PK) |
| `version_line` | 1,039,235 | 16.6s | **כן** |
| `link` | 5,798,418 | 13.9s | לא |
| `link_range` | 892,903 | 10.8s | **כן** |
| `link_anchor` | 1,717,073 | 6.4s | **כן** |
| `link_coverage` | 5,215,135 | 4.4s | לא (אינדקס מכסה) |
| `line_toc` | 5,888,227 | 3.9s | לא |
| `line_alt_toc` | 789,116 | 2.0s | **כן** |
| `tocEntry` | 1,209,229 | 1.4s | לא |

ה-hasher ממיין כל טבלה לפי עמודת `id`, ובהיעדרה לפי **כל** העמודות בסדר
אלפביתי — סדר שבחלק מהטבלאות אף אינדקס לא יכול לשרת, ואז SQLite כותב את
הטבלה כולה לקבצים זמניים וקורא אותה בחזרה (`USE TEMP B-TREE FOR ORDER BY`).
זה ~36s מתוך 133.5s. **לא** הפריט הגדול — `line` לבדה היא 73.6s של סריקה
טהורה — אבל זה עדיין רבע מהזמן.

**חשוב:** אסור לשנות את סדר המיון — הוא חלק מחוזה ה-hash מול הצד המפיק
ב-Kotlin. אפשר רק להוזיל אותו (שלב 4).

---

## 2. מה לא נוגעים בו — גבולות הבטיחות

* **אימות ה-hash נשאר.** הוא הדבר היחיד שמונע מסד שגוי בשקט. לא מוסר,
  לא הופך לאופציונלי, ולא נשלט מהגדרות.
* **חוזה ה-Kotlin נשאר.** זרם הבתים, סדר הטבלאות וסדר המיון — לא
  משתנים. מבחני ה-golden hash חייבים לעבור בלי שינוי בכל שלב.
* **`PRAGMA synchronous` לא נוגעים בו.** הוא מה שמגן על הפסקת חשמל
  באמצע כתיבה.
* **אין מצב שקט של "מסד לא נכון".** כל שינוי שמסיר בדיקה מנקודה אחת
  חייב להוסיף אותה במקום אחר, או לסמן את המסד כדורש הורדה מלאה.
* **אין גיבוי של המסד ואין הגדרה לו** — ראו AGENTS.md §5; זה לא משתנה.

---

## שלב 0 — מדידה על מסד אמיתי (חובה, לפני כל שינוי)

בלי זה לא נדע אם השקענו בצד הנכון: הפער בין "SHA איטי" ל"מיון חיצוני"
יכול להיות סדר גודל, לשני הכיוונים.

1. להשיג `seforim.db` אמיתי (~5GB) ושרשרת patches אמיתית.
2. להוסיף מכשור זמנים לכל תת-שלב — `PatchApplier.onStage` כבר מדווח
   אותם: `preflight`, `attach`, `migrations`, `upserts`, `deletes`,
   `foreignKeyCheck`, `verifyToHash`. להוסיף גם זמן וספירת שורות **לכל
   טבלה** בתוך `compute`.
3. להריץ `EXPLAIN QUERY PLAN` על הסכמה האמיתית — לאמת את הטבלה למעלה
   ולגלות טבלאות נוספות עם מיון.
4. פלט: טבלת זמנים בפועל. **היא שקובעת את סדר שלבים 1–5.**

מכשור הזמנים נשאר בקוד מאחורי דגל כבוי, כדי שנוכל למדוד גם אצל משתמש
שמדווח על איטיות.

---

## שלב 1 — SHA-256 נייטיבי ✅ בוצע

**רווח משוער: פי ~2.2 על שלב האימות** (מ-23 ל-~50 MB/s, אז ה-SQLite
נהפך לחוסם). מבוסס על מדידה, לא הערכה.

* `FastSha256` בחבילת השורש (`lib/src/services/fast_sha256.dart`) —
  `bcrypt.dll` ב-Windows, CommonCrypto (`libSystem`) ב-macOS, ונפילה חזרה
  ל-`package:crypto`. אותה תבנית `DynamicLibrary.open` שכבר עובדת
  ב-`ZstdFileDecompressor`.
* מוזרם דרך אותו `_BufferedByteSink` הקיים — **אפס שינוי בזרם הבתים**.
* משפר גם את אימות ה-sha256 של ההורדות ב-`PatchDownloader` (נכס של
  ~1.5GB) — מה שכבר מתועד ב-CHANGELOG כ"דקה ויותר".
* בטיחות: `test/fast_sha256_test.dart` משווה את שני המימושים על 12 גדלים
  (סביב גבולות 56/64/1MB), על BOM ובתי UTF-8 עבריים, על addSlice ועל שני
  sinks במקביל; ב-`logical_content_hasher_test.dart` נוסף golden שמריץ
  אותו מסד בשני המסלולים. בזמן ריצה יש בדיקה עצמית שמשתיקה מסלול נייטיבי
  שאינו מסכים עם `package:crypto`.
* סיכון: נמוך. נפילה חזרה למימוש הקיים אם ה-DLL חסר.

## שלב 2 — אימות אחד בסוף השרשרת, לא בכל צעד ✅ בוצע

**רווח: פי N, כאשר N = מספר ה-patches בשרשרת** (עד 9 במראה של 10
releases). זה החיסכון הגדול ביותר בתוכנית.

* `toContentHash` של הצעד האחרון מוכיח את כל השרשרת בעצמו.
* `PatchApplier.apply` קיבל `verifyToHash`, **ברירת מחדל פעיל** — כדי
  שהמנוע יישאר תואם לאוצריא. הכיבוי נעשה ב-
  `LibraryUpdateApplier.applyDelta`, שכבת התזמור שלנו.
* בטיחות — כאן המחיר האמיתי, וכך כוסה: שרשרת שנקטעה משאירה מסד שהוחל
  נקי אך לא אומת. `LibraryDbRecoveryService.markUnverified` רושם את
  גרסתו ב-`<db>.unverified`; ההחלה הבאה שמתחילה מאותה גרסה מפעילה
  `verifyFromHash` ומאמתת אותו **לפני** שהיא בונה עליו, ותוכן שגוי נדחה
  עם ההודעה הקיימת "נדרשת הורדה מלאה". הסימון נמחק אחרי אימות שהצליח,
  ואחרי התקנת מסד מלא (שממילא עובר `quick_check` + גרסה).
  * מסלול ההתאוששות שנשקל תחילה — לסמן את המסד כפסול ולהכריח
    `fullDownloadFallback` — נדחה: הוא היה שולח את המשתמש להורדה של
    ~1.5GB במקום למעבר אימות אחד, ובמקרה הנפוץ (הקטיעה הייתה סתם ביטול)
    המסד תקין לגמרי.
* לשרשרת של צעד אחד — הרוב המכריע של המקרים — אין שום הבדל התנהגותי.
* כיסוי: ארבע בדיקות ב-`library_manager/test/library_update_applier_test.dart`
  שמריצות שרשרת אמיתית עם patches דחוסים ב-libzstd — שני צעדים (אימות רק
  באחרון), צעד אחד, שרשרת שנקטעה וממשיכה, ומסד שסומן כלא-מאומת ותוכנו
  שונה (נדחה). ועוד שלוש ב-`test/patch_applier_test.dart` לדגל עצמו.

## שלב 3 — pragmas להחלה עצמה ⏸ לא אומץ: המדידה לא הכריעה

כרגע אין ב-`PatchApplier` אף pragma של ביצועים, וזו עדיין הזדמנות
תיאורטית. אבל המדידה שנעשתה (שש תצורות × שלוש טבלאות על המסד האמיתי)
**אינה שמישה**, ולא נכון להחליט לפיה:

| תצורה | `line` | `version_line` | `link_range` |
| --- | --- | --- | --- |
| ברירת מחדל | **27,329s** ⚠️ | 118.1s | 24.9s |
| `cache_size=256MB` | 112.9s | 111.6s | 8.8s |
| `mmap_size=1GB` | 104.2s | 25.5s | 25.8s |
| `threads=4` | 115.7s | 44.1s | 19.9s |
| `temp_store=MEMORY` | 107.7s | 42.4s | 23.1s |
| הכול יחד | 107.9s | 76.0s | 9.2s |

הריצה נמשכה 7.9 שעות ועברה דרך הלילה; הסבב הראשון לבדו לקח **7.6 שעות**
לעבודה שבמדידה נקייה ארכה 68 שניות — כלומר המכונה נכנסה לחיסכון בחשמל או
לתחרות דיסק. `line` יוצא 104–116s בכל התצורות (בלי הבדל), ו-`version_line`
מתנדנד פי 4 בלי תבנית עקבית. הרמז היחיד שנראה קוהרנטי: `link_range` הוא
~9s בשתי התצורות שכוללות `cache_size` מול ~20–26s בשלוש שלא — אבל אותו
`cache_size` יוצא *מזיק* ל-`version_line`, וזה סותר.

**המסקנה:** צריך למדוד מחדש — תצורות מתחלפות, כמה חזרות, על מכונה שלא
נכנסת לשינה — ולא להוסיף pragmas על סמך רעש. זה בדיוק הכלל שנקבע כאן
מראש ("מתקבל רק אם הוכיח שיפור"). `synchronous` נשאר כפי שהוא בכל מקרה
(ראו §2).

## שלב 4 — להוזיל את המיון החיצוני ❌ נדחה: עסקה גרועה (2026-08-23)

**ההצעה המקורית נשענה על עמודות שגויות.** `EXPLAIN QUERY PLAN` על המסד
האמיתי מראה שסדרי המיון חסרי-האינדקס הם:

| טבלה | סדר המיון בפועל | התוכנית *הניחה* |
| --- | --- | --- |
| `version_line` | `(charCount, **content**, lineId, versionId)` | `(lineId)` |
| `link_anchor` | `(charEnd, charStart, **label**, linkId, side)` | — |

אינדקס חייב להכיל את עמודות המיון שלו, ו-`content` הוא **749MB** של טקסט
ב-`version_line` — האינדקס היה משכפל את הטבלה, לא "50MB".

גם הגרסה החסכונית (אינדקס על העמודה המובילה בלבד, ~25MB, שהופך מיון מלא
למיונים בתוך קבוצה) שווה **~15s מתוך 168s — כ-9%**, ובתמורה:

* כל החלת patch נעשית איטית יותר — כל upsert/delete מתחזק גם את האינדקס.
  **התוכנית המקורית לא הזכירה את הצד הזה של המאזן בכלל.**
* האינדקס נמחק בכל הורדה מלאה וצריך להיבנות מחדש (מיון של מיליוני שורות).
* זה שינוי מתמשך בקובץ הספרייה של המשתמש, שגם אוצריא פותחת.

שתי האפשרויות האחרות נשארות פתוחות, שתיהן בחינם מבחינת מקום:
`PRAGMA threads` (תלוי במדידה מחדש של שלב 3) ווידוא שה-sorter אינו כותב
לכונן ה-USB.

### ואם בכל זאת — המקום הנכון הוא אוצריא, לא כאן

אימות בקוד המקור של אוצריא (`lib/migration/database/daos/database.dart`):
`MyDatabase.withPath` משמש גם ל-`seforim.db`, ובמסלול הכתיב הוא מריץ בכל
פתיחה את כל `_getCreateScripts()` — ובהם עשרות
`CREATE INDEX IF NOT EXISTS` (שורה 207). כלומר אוצריא **כן** מוסיפה
אינדקסים לקובץ הזה בעצמה.

לכן אינדקס כזה שייך לרשימה **שלה**, לא ל-updater: היא מקימה אותו מחדש בכל
פתיחה, כך שהוא שורד הורדה מלאה מעצמו. אינדקס שאנחנו נוסיף הוא שלנו
לתחזק, והוא נעלם בכל הורדה מלאה. אם השלב הזה יאומץ בעתיד — **זו בקשה
לאוצריא בשורה אחת**, לא שינוי כאן.

## שלב 5 — קידוד זול יותר בזרם הבתים ❌ נדחה: המדידה הפריכה

ההנחה הייתה ש-`utf8.encode(value.toString())` לכל תא מספרי הוא עלות
משמעותית. **הוא לא.** פירוק הצינור על מסד סינתטי (400 אלף שורות ב-`line`,
124MB) מראה:

| שלב | זמן | קצב |
| --- | --- | --- |
| קריאת ה-cursor לבד (בלי קידוד, בלי SHA) | 4,806ms | 28 MB/s |
| קריאה + קידוד לחוצץ (בלי SHA) | 4,893ms | 28 MB/s |
| **SHA-256 נייטיבי לבד על אותו נפח** | **96ms** | **1,417 MB/s** |
| הצינור המלא (`compute`) | 4,405ms | 29 MB/s |

הקידוד אינו נמדד כלל, וה-SHA הוא ~2% מהזמן. **כל** הזמן הוא קריאת השורות
דרך ה-FFI. מימוש של כתיבת ספרות ישר לחוצץ נכתב, נבדק ונמדד — ולא נתן
כלום, ולכן הוחזר לאחור ולא נשמר: אין סיבה להחזיק רוטינת int64→עשרוני
מקומית (עם מלכוד ‎-2^63) בלי תמורה.

**אזהרה למי שימדוד שוב:** בנצ'מרק שמחשב את נפח הבתים בתוך שלב ה-SHA מודד
בטעות את הסריקה פעמיים ומראה את ה-SHA כאיטי פי 15. זה קרה כאן.

## שלב 7 — בתי-הסוג כמסכה אחת לשורה ✅ בוצע

**רווח: 29.9% ממעבר האימות** (חציון של ארבעה סבבים מתחלפים; hash זהה).
זה השלב שנגזר ישירות מהמדידה של שלב 5 — הצוואר הוא **מספר קריאות ה-FFI
לכל שורה**, לא הקידוד ולא ה-SHA.

`_readValue` בחבילת `sqlite3` עושה שתי קריאות FFI לכל עמודה נבחרת
(`column_type` ואז הערך), ומקצה `List` ו-`Row` לכל שורה. ה-hasher בחר
**שתי** עמודות לכל עמודת DB — `typeof()` והערך — כלומר 24 קריאות FFI
לשורה ב-`line`, מהן מחציתן `sqlite3_column_text` שמקצה String ומפענח
UTF-8 רק כדי להגיד "integer".

מעכשיו בתי-הסוג נבחרים ארוזים: 3 ביטים לעמודה, 20 עמודות למספר שלם אחד
(60 ביט — חיובי ב-int64 גם ב-SQLite), ומפוענחים בדארט בהזזה ומסכה.
טבלה רחבה מ-20 עמודות מקבלת מסכה נוספת, בלי גבול עליון.

* **זרם הבתים אינו משתנה בבית אחד.** הערכים 0/1/2/3 שנארזים הם בדיוק
  `_nullTag`/`_blobTag`/`_numberTag`/`_textTag`. ביטוי הערך
  (`CAST AS BLOB` ל-text, לשמירת BOM) לא נגעו בו, וגם לא ה-`ORDER BY`.
* בטיחות: `referenceStream` במבחנים עדיין קורא `typeof()` כמחרוזת ולכן
  הוא **עד עצמאי** לזהות הבתים. נוספו מבחנים לגבול המסכה
  (1/19/20/21/40/41 עמודות), למיון האלפביתי של שמות העמודות (`c10` לפני
  `c2` — הסדר שבו התגים נארזים חייב להיות של העמודות הממוינות), ולכל
  חמשת סוגי SQLite כולל `-0.0`/`Infinity`/`NaN`. ה-A/B על 400 אלף שורות
  אימת hash זהה בין שני המסלולים.
* מסלול כשל: מסכה שגויה נותנת hash שגוי, כלומר **נכשלת בקול** ב-ROLLBACK
  עם ההודעה הקיימת — לא משתיקה מסד שגוי.

### אישור על המסד האמיתי (2026-08-23)

זו המדידה שדרש שלב 0. `tool/measure_hash.dart` על המסד האמיתי (7.9GB
קובץ, `line` בת 5,910,542 שורות) — **סך הסריקה 168.0s** בצורת ה-SELECT
**הישנה**, כלומר זהו קו הבסיס:

| טבלה | זמן | שורות | נפח לוגי |
| --- | --- | --- | --- |
| `line` | 46.6s | 5,910,542 | 4,431MB |
| `link` | **45.0s** | 5,801,217 | **184MB** |
| `version_line` | 19.5s | 1,038,484 | 749MB |
| `link_range` | 13.4s | 894,424 | 14MB |
| `link_anchor` | 12.4s | 1,719,801 | 23MB |
| `link_coverage` | 12.0s | 5,218,360 | 64MB |
| `line_toc` | 10.2s | 5,909,959 | 73MB |
| `tocEntry` | 4.3s | 1,212,992 | 35MB |
| `line_alt_toc` | 3.5s | 789,115 | 11MB |

**`link` היא ההוכחה הישירה:** 45 שניות על 184MB — 4 MB/s. הזמן אינו
נשרף על נפח אלא על **מספר התאים**. הטבלאות שנשלטות ע"י מספר תאים ולא ע"י
נפח (`link`, `link_coverage`, `line_toc`, `tocEntry`, `link_anchor`,
`link_range`, `line_alt_toc`) הן ~100s מתוך 168s.

A/B על תשע הטבלאות הכבדות (167s מתוך 168s), שלושה סבבים בסדר מתחלף:

| סבב | ישן | חדש |
| --- | --- | --- |
| 1 | 106s | 76s |
| 2 | 105s | 71s |
| 3 | 124s | 100s |

חציון: **106s → 76s, מהיר ב-28.3%** — תואם ל-29.9% שנמדד על המסד
הסינתטי, ובאותו כיוון בכל שלושת הסבבים.

### מה נשאר בקריאה, ולמה זה הגבול

אחרי השלב הזה נשארת קריאת FFI אחת לכל תא ועוד אחת לשורה. הורדה נוספת
דורשת לקרוא שורה שלמה בקריאה אחת — אין API כזה ב-`package:sqlite3`.
שרשור הערכים ל-blob אחד בתוך SQLite **אינו** מסלול: `||` הוא אופרטור
טקסט וקוטע ב-0x00, ו-`unhex(hex(a)||hex(b))` מכפיל את הנפח.

## שלב 6 — מסלול ההורדה המלאה: `quick_check` ⏸ נשאר, עד שינוי אצל המפיק

**נמדד (2026-08-23): 111s** על המסד האמיתי (7.4GB). כלומר כן מדובר בדקות,
והפרס אמיתי.

**אבל ההנחה של התוכנית — "כשה-sha256 של הנכס הדחוס אומת, ה-quick_check
אינו מוסיף מידע" — שגויה.** מעקב על מסלול ההורדה המלאה
(`LibraryUpdateApplier._applyFullDb`) מראה:

* `ZSTD_decompressStream` תופס קלט פגום, ו-`lastResult != 0` תופס frame
  קטוע — כלומר **הקלט** מכוסה.
* **אין שום אימות sha256 על הקובץ המחולץ** (~7.4GB). ל-patches יש
  `uncompressedSha256` ב-manifest; למסד המלא אין manifest כזה בכלל, רק
  `ReleaseAsset.digest` של הקובץ **הדחוס**.
* `_sha256FromDigest` מחזיר `null` כשאין digest או כשהוא לא בפורמט
  `sha256:` — כלומר יש מצבים שבהם גם הדחוס אינו מאומת.
* מספר הבתים שנכתבו בפועל **אינו** מושווה ל-`ZstdFileDecompressor.
  contentSizeOf` (שנקרא, אך משמש רק לבדיקת מקום פנוי).

לכן `quick_check` הוא הבדיקה **היחידה** מקצה-לקצה על הקובץ המחולץ, והיא
תופסת בדיוק את מה שהשאר לא: תקלת כתיבה לדיסק. **הוא נשאר.**

**ואוצריא עצמה מסכימה.** `LibraryUpdateRepository.applyFullDownload`
(`lib/library_update/repository/library_update_repository.dart`) עושה את
אותו דבר בדיוק: הורדה עם `expectedSha256` מה-digest (אופציונלי), חילוץ,
ואז `PRAGMA quick_check` + בדיקת גרסה ב-isolate (שורה 426) לפני החלפה
אטומית. אין שם אימות sha256 על המחולץ. הסרת ה-`quick_check` כאן הייתה
גם מחלישה בטיחות **וגם סוטה מאוצריא** — שני דברים שאסורים לפי §2.

וגם מאושר מהמסמך של אוצריא (`docs/seforim_library_delta_update_plan.md`):
ה-release מפרסם `seforim.db.zst` **בלי manifest כלל** — `uncompressedSha256`
קיים רק ב-manifest של patch. לכן ההחלפה ב-sha256 היא בקשה ל-
`Otzaria/SeforimLibrary` (צד הייצור), לא לאוצריא ולא לכאן.

**הדרך הנכונה לגבות את 111 השניות** היא לא להסיר בדיקה אלא להחליף אותה
בחזקה ממנה: אם `SeforimLibrary` יפרסם `uncompressedSha256` גם לנכס המסד
המלא (המכניזם קיים — הוא עושה זאת ל-patches), אימות sha256 נייטיבי על
7.4GB לוקח **~5s** ב-1,417 MB/s, והוא תופס גם שינוי בית בודד בתוך התוכן
— מה ש-`quick_check` מפספס. **זה דורש שינוי בצד המפיק, לא כאן.**

### הקשחה קטנה שנעשתה כאן, בחינם ✅ בוצע

מספר הבתים שנכתבו לא הושווה לגודל שה-frame מצהיר עליו; עכשיו כן.
**הערכה מדויקת של הערך:** זו אינה סגירת פרצה — libzstd עצמו כבר תופס קלט
פגום או קטוע, ו-`writeFromSync` של דארט זורק בכשל כתיבה. מה שנוסף הוא
שכבת הגנה על הקובץ **כפי שהוא על הדיסק**: אנטי-וירוס שקיצר אותו, מערכת
קבצים נשלפת שדיווחה הצלחה. שיפור אמיתי אך צנוע, בעלות אפס.

רק `<` נחשב כשל: `contentSizeOf` קורא את ה-frame הראשון בלבד, וארכיון
מרובה-frames מפיק יותר ממנו — `!=` היה דוחה חילוץ תקין לגמרי. יש מבחן
ששומר על ההבחנה (אומת ע"י מוטציה: עם `!=` הוא נופל).

---

## מה לא בתוכנית, ולמה

* **להפעיל `verifyFromHash`** — היה מוסיף מעבר מלא נוסף. הוא כבוי
  בכוונה, כמו באוצריא.
* **להקביל את ה-SHA-256** — האלגוריתם סדרתי מעצם טבעו. אפשר לפצל את
  ייצור הבתים וה-hash לשני isolates בצינור (עד פי 2), אבל שלב 1 מוריד
  את ה-SHA מהמסלול הקריטי וזה נהפך למיותר. שלב 7 מאשר: ה-SHA הוא ~2%.
* **להקביל את *הקריאה* לטווחי `id` בכמה isolates** — נשמע כמו הרווח
  הגדול (הקריאה היא 98% מהזמן, ו-`line` ממוינת לפי `id` כך שפיצול
  לטווחים היה זהה בית-בית). **חסום, ולא בגלל האלגוריתם:** `verifyToHash`
  רץ בתוך ה-transaction הפתוח של ההחלה (`BEGIN` בשורה 208 של
  `patch_applier.dart`, `COMMIT` ב-255), ו-isolate עם חיבור נפרד **אינו
  רואה** נתונים שלא נעשה להם commit. להזיז את האימות אחרי ה-COMMIT היה
  פותח את המקביליות — ומוותר על ה-ROLLBACK שהוא כל הערך של האימות: אי-
  התאמה כבר לא הייתה משאירה את המסד של המשתמש כמו שהיה. זה ויתור על
  איכות, ולכן לא נעשה.
* **לקצר את זמן ההורדה** — זו הרשת, לא הקוד.
* **לדלג על עדכון הקבצים הנלווים** — הם קטנים ואוצריא בודקת אותם.

---

## סדר עבודה

לפי AGENTS.md §3: בכל שלב, ובכל חבילה שנגעו בה — `dart format .`, ואז
`flutter analyze --no-fatal-infos` (או `dart analyze` בחבילות דארט
הטהורות), ואז המבחנים. שלב שמשנה לוגיקה מקבל מבחנים חדשים, לא בדיקה
ידנית. כל שלב = commit נפרד + פסקה ב-CHANGELOG.

**דחיפה ירוקה ל-`main` מפרסמת גרסה** (AGENTS.md §3), ולכן כל שלב נדחף
בנפרד ורק כשהוא שלם — לא כחלק מ"התוכנית כולה".

## מצב נוכחי (2026-08-23)

| שלב | מצב | רווח |
| --- | --- | --- |
| 1 — SHA-256 נייטיבי | ✅ בוצע | פי 3 על מעבר האימות (נמדד) |
| 2 — אימות אחד בסוף השרשרת | ✅ בוצע | פי מספר ה-patches בשרשרת |
| 7 — בתי-הסוג כמסכה לשורה | ✅ בוצע | **28.3% על המסד האמיתי** (נמדד) |
| 3 — pragmas | ⏸ המדידה לא הכריעה | לא ידוע |
| 4 — המיון החיצוני | ❌ נדחה — עסקה גרועה | ~9%, במחיר החלה איטית יותר |
| 5 — קידוד זול יותר | ❌ נדחה — המדידה הפריכה | אפס |
| 6 — `quick_check` | ⏸ נשאר — הוא לא מיותר | 111s, רק בשינוי אצל המפיק |

**המסקנה המרכזית מהסבב של 2026-08-23:** אחרי שלב 1, מעבר האימות הוא
**כולו** קריאת שורות דרך ה-FFI — לא SHA (2%) ולא קידוד (0%). כל שלב הבא
צריך להקטין את מספר קריאות ה-FFI או את מספר השורות הנקראות, ולא לייעל
חישוב. שלב 7 מימש את הראשון עד הגבול שה-API מאפשר.

מה שנשאר:

* **שלב 3** דורש **מכונת מדידה שקטה** — לא הוכרע רק בגלל זה.
* **שלב 6** — הפרס (111s) אמיתי, אבל הוא נגבה רק אם `SeforimLibrary`
  יפרסם `uncompressedSha256` לנכס המסד המלא. **זו בקשה לצד המפיק.**
* **סגירת פער הגודל בחילוץ** — חינם, ומחזק בטיחות. לא נעשה עדיין.

**אזהרה למי שממשיך מכאן:** שלושה מתוך ששת השלבים המקוריים התבססו על
הנחות שהמדידה הפריכה — שלב 5 (הקידוד יקר), שלב 4 (עמודות המיון), ושלב 6
(ה-quick_check מיותר). לפני כל שלב נוסף: **למדוד קודם, על המסד האמיתי,
בסבבים מתחלפים.** זה בדיוק מה ששלב 0 דרש מההתחלה.
